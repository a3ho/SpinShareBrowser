# SpinShare Browser

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

在 Windows 上查找、筛选、阅读和安装 SpinShare 谱面的用户。

## Product Purpose

在一个桌面工具中完成谱面筛选、查看作者信息与评论、下载和安装。界面应便于连续浏览和比较，而不是以宣传内容为主。

## Operating Context

Python 提供本地服务和下载安装功能，WebView2 承载 HTML/CSS/JavaScript 界面。现有中英文界面、窗口控制、设置和本地安全策略需要保留。用户在 2026-09-01 同意先实现一张代表性卡片及整页的可交互新界面，再审核后逐步替换；无需重新选择框架。

## Capabilities and Constraints

- 保持查询不会增加官网谱面浏览量，不请求会增加浏览量的曲目详情接口。
- 每张谱面只在封面上提供试听按钮，点击后使用顶栏内唯一的全局播放器；卡片不嵌入独立播放器。歌名是可选择、复制的普通文本，不再承担跳转；官网入口由标题旁独立、低调的外链图标提供。
- 单次试听最多 25 秒。顶栏播放器显示较大封面、歌名与艺人、当前时间、总时间和可拖动进度，不显示难度。已有曲目时，Space 全局切换播放 / 暂停，但编辑输入和原生以 Space 操作的控件优先；没有当前曲目时不拦截 Space，也不根据悬停谱面推断要播放的内容。空格确实改变播放状态时，顶栏封面短暂显示新的播放 / 暂停状态后自行淡出；等待、拒绝、重复键或其它没有实际改变媒体状态的情况不显示反馈，进度条也不因全局快捷键保留焦点框。
- 界面不使用 HTML `title` 或等价的原生悬停小字提示。图标按钮和省略内容仍保留明确的可见状态与 `aria-label` / `aria-description`，不能用取消悬停提示为由删掉无障碍名称；用户明确要求的标签完整列表、评论和描述阅读浮层不属于原生 tooltip。
- 音频地址只能由通过严格校验的 `fileReference` 拼接为 `https://spinshare.b-cdn.net/uploads/audio/{fileReference}_0.ogg` 或 `.mp3`，不得读取曲目详情、下载或其它计数接口。OGG 失败或 15 秒内未就绪时只回退一次 MP3；MP3 同样受 15 秒加载边界约束，失败后停在可重试状态，不能循环切换格式。
- 筛选、文字搜索、标签、排序和翻页保留当前试听；全量目录更新若发现曲目、`fileReference` 或媒体版本身份已变化，则释放旧媒体。页面或宿主隐藏时暂停，恢复可见后不自动续播；退出与页面卸载时停止并释放音频、监听器和计时器。播放器与封面切换使用统一短动效，顶栏双封面槽位交叉淡化；减少运动偏好取消空间位移和缩放。
- 全量目录及前台、后台状态持久保存在应用状态目录的 `charts-cache.json`，默认位于 `%LOCALAPPDATA%\SpinShareBrowser`，与程序安装目录和谱面目标目录独立，正常完全退出后保留。前台手动更新完成 DNS、TCP 与 TLS 连接后、发送目录请求前，必须先持久保存本次请求及十分钟冷却；写入失败不得联网。明确的 401／403 访问拒绝在安全回滚该记录后不占冷却；限流、服务器异常、等待响应超时、请求或正文中断、响应过大／无效及最终落盘失败均保留冷却，重开窗口或进程不能绕过。只有确认请求尚未到达服务器的连接阶段失败、本机预检失败，以及已成功回滚的访问拒绝允许立即重试。旧缓存格式没有失败成本分类，升级时必须保守迁移。新目录先原子落盘、成功后再发布；写失败不能以仅存在于内存的新数据冒充已保存目录。
- 筛选、文字搜索、标签、排序和翻页严格读取本地目录，不因操作本身访问 SpinShare。没有本地目录或最近一次成功 `fetchedAt` 已超过 12 小时时，启动流程先执行一次自动同步；运行期间达到 12 小时也以单任务、低优先级在后台同步。自动通道不写前台十分钟冷却，使用独立持久退避（5 分钟、15 分钟、1 小时、6 小时）；退避只限制自动任务，用户在错误卡片中主动“重试”仍作为前台手动请求处理。前台冷却尚未结束时，自动任务不得成为绕过限制的后门。
- 启动自动同步期间用中央聚焦卡片阻止进入旧状态，显示真实阶段和已接收数据量；成功后自动退场。失败但有旧目录时提供“重试”和“使用本地数据”，没有目录时提供“重试”和“退出”。运行期间的后台同步不改变当前筛选或阻塞本地操作；托盘且无前台窗口时，实际更新或失败使用不抢焦点的右下角轻提示，数据未变化则保持安静。手动“更新数据”保留十分钟规则，并在旁边解释 12 小时自动同步，因此多数情况下无需手动操作。
- 更新失败必须区分网络不可达、访问拒绝、限流、服务器异常、请求／传输超时或中断、响应过大／无效与本地存储故障。只有能够确认几乎没有占用服务器资源的前台失败允许立即再次联网；请求已发送、可能到达服务器或已开始返回全量数据时，前台十分钟内只返回本地冷却。不能用空表或成功掩盖失败。取得新目录后失效相关派生缓存、重建结果与候选计数，目录更新本身不清空标签、关键词或其它搜索选择。
- 标签是独立筛选维度；改变其它条件不清空标签。首次筛选前，搜索和标签控件不可用。
- 结果工具栏提供安装状态筛选，默认“全部”，可选“仅已安装”或“仅未安装”；首次有效筛选前不可用。它与难度、日期、关键词范围、AND 标签和排序组合，改变其中一项不重置其它项。
- 安装状态筛选覆盖整个候选集合，不只处理当前页；通过本地 `/v1/installations/check` 核对，不新增远程全量刷新。未知或核对失败不能作为“未安装”，应保留明确的未确认状态和错误提示。
- 标签联想仅来自已有标签，忽略大小写；显示附加候选后剩余的结果数。
- 回车和卡片标签点击均可附加标签；已有标签可删除。删除或清空不会跳回来源卡片。
- 已选标签栏只有非空时显示，在搜索下方自然布局，滚动后吸顶；卡片点击附加保留飞行动效。
- 作者说明不参与搜索，卡片保持紧凑布局且不显示“描述”标题，不再内嵌滚动。短说明完整显示，无光晕或展开交互，原生链接可用；超过预览容量的长说明必须露出实际文字的标准半行，并额外保留最后 3px 渐隐余量提示全文，避免渐隐遮淡半行主体或切在段落空行。长预览在悬停、键盘聚焦或打开期间，以实体表面和文字抬升 2px、背后扩散柔光表达层次；外部命中区域不动，普通悬停不显示亮色轮廓，键盘保留边缘焦点提示，减少运动时取消位移。绝不悬停自动打开；只能点击、Enter 或 Space 打开，预览内容为 inert，选择、复制和链接操作留在全文浮层。
- 描述全文浮层只显示正文，不添加可见歌名或“描述”标题，仅由 aria-label 提供曲目上下文；评论浮层标题保持不变。全文复用已加载的说明和既有安全链接，不新增服务器请求，与临时评论及标签浮层协调。整片浮层的普通滚轮都只滚动内部，包括空白、边缘、短内容及滚动边界，Ctrl+滚轮保留原有行为；沿用轻度背景压暗和 220ms 浮出 / 150ms 退回，阅读期间停稳，减少运动时无空间动画。不设 X；离开来源谱面卡片与浮层后保留 160ms 容错再关闭，Escape 或外部点击也可关闭。离开判定使用来源卡片和顶层浮层的真实 `pointerenter` / `pointerleave` 联合区域，不依赖可能在 Chromium top layer 中滞留的 CSS `:hover`。页面滚动只在首次事件启动同一退场，连续滚轮或惯性滚动不得反复重启动画，开合不改变文档高度。
- 卡片标签独立留在说明底部一行，不触发描述展开。溢出时悬浮面板展示全部标签，面板中的标签可以点击；支持键盘和触屏。
- 评论默认全部收起，但计数不依赖展开：当前整页、连续模式已渲染批次自动进入现有最多 2 并发的后台队列，优先可见或正在阅读的卡片。数量缓存与全文 DOM 独立，关闭评论不取消计数；失败保留已有数量，没有已知数量时明确显示可重试的未知状态，只有确认无评论才显示 0。已确认总数为 0 时保留统一的评论按钮外形，但禁用展开、隐藏箭头且无悬停高亮，全局展开也跳过；有条目的纯评分仍可展开。若打开后才确认 0，安全关闭并清除旧正文，焦点原在评论区域或数量按钮时回到本卡标题且不滚动页面。
- 所有谱面的手动“更新评论”共用至少 60 秒冷却，失败也计入；各按钮显示同一倒计时，由单一计时器更新。首次加载数量和打开缓存不占冷却，也不会因冷却停止；手动刷新仍通过现有队列执行，不能靠排队、换卡片或重新展开绕过间隔。前端将时间戳保存在同源 localStorage，刷新页面不会清空；这不代表不同端口或独立进程共享的后端强制限流。
- 每张卡片统计中的评论数量是按钮，点击后打开单个临时评论浮层。评论节点仍属于原卡片，呈现使用原生 `popover=manual` 顶层和固定位置，不占文档布局、不锁定页面滚动；打开、加载全文和收起都不改变页面高度或推动下方卡片。
- 宽窗浮层靠近评论按钮并限制在视口内，不留难以跨越的间隙；宽度不超过 680px 时延伸至近视口底部，上沿避开顶栏并兼顾按钮位置，保持鼠标可达。浮层内任何位置的普通滚轮都只滚动内部列表，包括标题、摘要和空白处；到顶、到底或内容较短时也不带动外层，Ctrl+滚轮和全局内嵌评论保留原有行为。轻度压暗背景与浮层深度突出阅读焦点，开合时短暂浮出与退回，阅读期间停稳，减少运动时不做空间动画。不设右上关闭键；离开来源卡片和浮层、再次点击数量按钮、Escape、外部点击或换页均可收起，必要的焦点返回不滚动页面。
- 全局“展开所有评论”开关默认关闭。开启后完整评论内嵌于各卡片，随全文延伸卡片且不随鼠标离开收起；关闭后恢复按需浮层。两种呈现只通过位置、背景和层级区分，不显示模式文字标识。完整内容始终可达，不截断、不改成摘要，也不迁入独立详情页。
- 评论时间不再显示“· Europe/Berlin”等时区尾缀，但保留原有日期换算。
- 保留下载队列、并发控制、安装状态、错误解释、目录选择和安装保护。
- 开发留在 codex 分支；用户审核前不合并 main。
- 应用只有谱面浏览这个主页面，不设常驻侧栏；官网、官方客户端和社区作为页脚补充链接。内容不足一屏时页脚必须落在应用可视底边，内容增长后随文档自然下移，不能固定悬浮或遮挡谱面。
- 尚未查询、正在加载、查询失败、没有匹配结果或结果不超过一页时，不显示没有作用的上下分页；状态提示在筛选区与页脚之间的可用内容区域居中，避免成为悬在页面上半部的孤立文字。结果区不再显示重复的安装说明；无限显示模式只在结果上方保留紧凑的显示数量选择入口，以便切回分页。
- 查询期间由结果舞台提供唯一的加载文字与旋转反馈；筛选区不重复显示加载消息，筛选按钮不旋转。难度、日期、重置和提交控件在请求完成前锁定，也不提供可反复触发请求与取消的取消按钮。接收数据的字节进度继续更新同一个中央状态。
- SpinShare Browser 是同一层级的完整名称，两部分字号、字重、颜色一致。
- 不显示重复说明页面用途的“谱面浏览”大标题；首屏直接呈现筛选，分组保留一层可见标签，统一紧凑的控件与间距。
- 默认和自定义日期状态下筛选分组都从顶部对齐；日期选择保留手输、开放区间与快捷范围，并提供清晰的年月导航。

## Brand Commitments

保留 SpinShare Browser 名称、现有标志与识别色。用户喜欢 Epic 游戏客户端的界面感觉，已同意以克制的深色层次、清晰封面和标题、统一控件及短促动效为方向，同时保留谱面工具的信息密度。

## Evidence on Hand

现有代码、用户提供的官网截图及当前界面截图、项目内测试与离线 fixture。离线预览中的示例数据必须明确为测试数据，不作为真实下载量或评价发布。已有图片不替换成生成的曲目封面。

## Product Principles

- 保留搜索、标签、评论全文可达和安装能力；评论呈现按本次明确授权调整。
- 把相关信息放在一起，让歌曲识别和安装操作容易找到。
- 长内容可读、可达；评论收起只改变展示状态，不删减完整内容。
- 渐进替换前台，复用后端，不为使用设计技能引入框架。

## Open Decisions

本轮已明确授权持久目录缓存、12 小时自动同步、独立后台退避、前台“更新数据”十分钟冷却、启动聚焦卡片与托盘不打扰提示；长说明的半行渐隐预览与主动打开全文浮层、取消按评论数量排序、后台评论计数、逐卡与全局展开、无模式文字标识、无时区尾缀和独立安装状态筛选继续保留。整体视觉方案仍待用户审核；这些授权不代表最终视觉批准或允许合并 main。

## Superseded Decisions

- 早期默认在卡片中完整展开评论的要求已被默认收起及全局展开开关替代。
- 先前的“数量按钮打开临时内嵌抽屉，开合高度动画推移下方卡片”方案，以及“临时展开 / 常驻展开”文字标识，均为 **superseded**。它们不能作为本轮实现依据；当前临时阅读使用不占页面布局的浮层，全局展开仍内嵌。
- 先前收起时不请求评论、打开后才获得数量的实现已被 **superseded**；当前评论数量独立后台加载和缓存，未知或失败不能显示为 0。
- 先前作者说明在卡片内滚动的方案已被 **superseded**；当前短说明完整显示，长说明用半行渐隐预览并通过主动操作打开全文，不以悬停打开。
- 先前由筛选或“刷新列表”隐式决定是否联网的实现已被 **superseded**；当前所有筛选只读本地目录，超过 12 小时由独立自动通道同步，显式“更新数据”才进入前台十分钟规则。评论手动刷新的 60 秒倒计时不受此变化影响。
