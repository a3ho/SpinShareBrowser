# SpinShare Browser

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

在 Windows 上查找、筛选、阅读、安装和管理本地 SpinShare 谱面的用户。

## Product Purpose

在一个桌面工具中完成谱面筛选、查看作者信息与评论，以及下载、安装和删除本地谱面。界面应便于连续浏览和比较，而不是以宣传内容为主。

## Operating Context

Python 提供本地服务及谱面下载、安装和删除功能，WebView2 承载 HTML/CSS/JavaScript 界面。2.0.0 已确立为当前正式产品基线；现有中英文界面、窗口控制、设置、本地安全策略和完整交互均在这套架构中继续维护，无需重新选择框架。

## Capabilities and Constraints

- Windows 顶栏空白拖动和窗口边缘缩放由宿主维护一个透明原生命中层，保留系统移动、缩放和双击最大化 / 还原；品牌、右侧控件和播放器的封面按钮、进度条、重试按钮必须透传，播放器其余文字与背景可拖动。播放器右侧始终保留至少 32px 拖动空白，窄窗时播放器自适应让出空间，不缩小操作按钮。禁用 WebView2 动态非客户区，避免播放器更新触发窗口位置消息循环。布局只在尺寸或可见状态变化时合并上报；旧 DPI / 视口布局不得覆盖新位置的控件，模态对话框打开时取消顶栏拖动区域。
- 保持查询不会增加官网谱面浏览量，不请求会增加浏览量的曲目详情接口。
- 每张谱面只在封面上提供播放按钮，点击后使用顶栏内唯一的全局播放器播放完整歌曲；卡片不嵌入独立播放器。歌名是可选择、复制的普通文本，不再承担跳转；官网入口由标题旁独立、低调的外链图标提供。
- 播放器采用谱面包中的完整歌曲音频和媒体实际时长，不做人为试听截断。顶栏播放器显示较大封面、歌名与艺人、当前时间、总时间和可拖动进度，不显示难度。已有曲目时，Space 全局切换播放 / 暂停，左右方向键分别后退 / 快进 5 秒；编辑输入及具有原生键盘行为的控件优先。没有当前曲目时不拦截这些按键，也不根据悬停谱面推断要播放的内容。首次点击播放时会显示一次性快捷键提示。空格确实改变播放状态时，顶栏封面短暂显示新的播放 / 暂停状态后自行淡出；等待、拒绝、重复键或其它没有实际改变媒体状态的情况不显示反馈，进度条也不因全局快捷键保留焦点框。
- 界面不使用 HTML `title` 或等价的原生悬停小字提示。图标按钮和省略内容仍保留明确的可见状态与 `aria-label` / `aria-description`，不能用取消悬停提示为由删掉无障碍名称；用户明确要求的标签完整列表、评论和描述阅读浮层不属于原生 tooltip。
- 正常音频由通过严格校验的 `fileReference` 拼接 CDN `_0.ogg` 或 `_0.mp3`，不得读取曲目详情、下载或其它计数接口。OGG 失败或 15 秒内未就绪时只回退一次 MP3；两者均失败时，才通过认证的本地解析接口按需读取静态 SRTB 的 `ClipInfo_0`。如果它引用游戏音乐，以精确 GUID 查找本机 Steam 主库、多库及已安装 DLC 中的 OGG，只读直传并支持 Range 定位，不复制、转码或修改游戏文件。未安装所需资源时明确提示检查游戏安装；其它资源或网络失败保留准确原因与重试，不循环尝试。正常 CDN 播放不增加静态谱面请求。
- 本地音频解析与安装队列分离，解析请求独立 20 秒超时，可取消；本地媒体只通过认证解析后发放的单文件随机短期地址访问，不暴露文件系统路径。解析成功缓存必须绑定 `fileReference`、`updateHash` 与设置版本；切歌、目录变更或谱面版本变化不能复用旧曲目结果。迟到结果不得覆盖新曲目，暂停或隐藏后不得自动播放。
- 筛选、文字搜索、标签、排序和翻页保留当前播放；谱面数据更新若发现曲目、`fileReference` 或媒体版本身份已变化，则释放旧媒体。页面或宿主隐藏时暂停，恢复可见后不自动续播；退出与页面卸载时停止并释放音频、监听器和计时器。播放器与封面切换使用统一短动效，顶栏双封面槽位交叉淡化；减少运动偏好取消空间位移和缩放。
- 谱面数据及前台、后台状态持久保存在应用状态目录的 `charts-cache.json`，默认位于 `%LOCALAPPDATA%\SpinShareBrowser`，与程序安装目录和谱面目标目录独立，正常完全退出后保留。前台手动更新完成 DNS、TCP 与 TLS 连接后、发送谱面数据请求前，必须先持久保存本次请求及十分钟冷却；写入失败不得联网。明确的 401／403 访问拒绝在安全回滚该记录后不占冷却；限流、服务器异常、等待响应超时、请求或正文中断、响应过大／无效及最终落盘失败均保留冷却，重开窗口或进程不能绕过。只有确认请求尚未到达服务器的连接阶段失败、本机预检失败，以及已成功回滚的访问拒绝允许立即重试。旧缓存格式没有失败成本分类，升级时必须保守迁移。新谱面数据先原子落盘、成功后再发布；写失败不能以仅存在于内存的新数据冒充已保存数据。
- 筛选、文字搜索、标签、排序和翻页严格读取本地谱面数据，不因操作本身访问 SpinShare。没有本地谱面数据或最近一次成功 `fetchedAt` 已超过 12 小时时，启动流程先执行一次自动同步；运行期间达到 12 小时也以单任务、低优先级在后台同步。自动通道不写前台十分钟冷却，使用独立持久退避（5 分钟、15 分钟、1 小时、6 小时）；退避只限制自动任务，用户在错误卡片中主动“重试”仍作为前台手动请求处理。前台冷却尚未结束时，自动任务不得成为绕过限制的后门。
- 启动自动同步期间用中央聚焦卡片阻止进入旧状态，显示真实阶段和已接收数据量；成功后自动退场。失败但有旧谱面数据时提供“重试”和“使用本地数据”，没有本地谱面数据时提供“重试”和“退出”。运行期间的后台同步不改变当前筛选或阻塞本地操作；托盘且无前台窗口时，实际更新或失败使用不抢焦点的右下角轻提示，数据未变化则保持安静。手动“更新数据”保留十分钟规则，并在旁边解释 12 小时自动同步，因此多数情况下无需手动操作。
- 更新失败必须区分网络不可达、访问拒绝、限流、服务器异常、请求／传输超时或中断、响应过大／无效与本地存储故障。只有能够确认几乎没有占用服务器资源的前台失败允许立即再次联网；请求已发送、可能到达服务器或已开始返回完整谱面数据时，前台十分钟内只返回本地冷却。不能用空表或成功掩盖失败。取得新谱面数据后失效相关派生缓存、重建结果与候选计数，谱面数据更新本身不清空标签、关键词或其它搜索选择。
- 数据更新失败使用结构化卡片：标题先说明用户当前能做什么，再用“重试状态”“可重试时间”与“当前数据”等键值行呈现必要信息；不向用户暴露传输阶段、内部请求名称或服务器资源占用等实现措辞。所有问号帮助在鼠标悬停或键盘聚焦时出现，移开或失焦即消失，不依赖点击；浮层方向避开相邻操作，今后新增问号沿用同一规则。
- 标签是独立筛选维度；改变其它条件不清空标签。首次筛选前，搜索和标签控件不可用。
- 结果工具栏提供安装状态筛选，默认“全部”，可选“仅已安装”或“仅未安装”；首次有效筛选前不可用。它与难度、日期、关键词范围、AND 标签和排序组合，改变其中一项不重置其它项。
- 结果工具栏中的安装状态、排序依据和排序方向必须完整显示中英文当前值及全部下拉选项，不使用省略号或缩写。横向空间不足时让完整字段组提前换行并保持字段名与控件相邻，不能依靠裁切控件文字维持单行。
- 已有筛选结果后修改难度、等级或日期时，不再用可见段落挤占筛选操作列；原提交按钮保持原位并从“筛选谱面”切换为“应用更改”，应用成功后恢复。完整状态只在屏幕阅读器专用的 `aria-live` 区域播报，宽窄窗口与中英文切换都不能改变筛选面板高度或使按钮换行移位。
- 安装状态筛选覆盖整个候选集合，不只处理当前页，也不触发服务器谱面数据更新。窗口重新获得焦点时仍自动核对；常规完整刷新由本地 `/v1/installations/index` 一次枚举当前安装目录实际存在的 `.srtb` 文件并返回 `fileReference` 与内容哈希清单，前端只在内存中把当前 catalog 与清单匹配，不再按 catalog 候选逐一探测文件。
- 单首安装完成后通过 `/v1/installations/check` 定向核对该谱面的本地文件与哈希并立即更新卡片，不等待其它任务结束；失败才退回常规清单刷新。删除完成只使对应谱面的清单匹配缓存失效并更新，安装目录变更使旧目录的整份清单失效后针对新目录重新核对。DLC 与普通谱面使用同一套本地安装状态、进度、筛选、安装、重新安装和删除链路；无效 `fileReference` 或哈希一律按普通未知状态处理，不能因 DLC 身份特殊归类。
- DLC 卡片在歌曲信息区显示“需要 {DLC 名称}”要求标识，不把它伪装成安装错误。有严格合法的 `https://store.steampowered.com/app/...` 地址时，整枚标识可由用户操作并在系统浏览器打开对应商店；名称或地址异常时退化为不可点击的“需要 DLC”，且不阻塞谱面安装、核对或删除。完整 DLC 元数据在本地压缩行中保留，不能再压成布尔值。
- 清单核对真正失败时，不能把缺少新结果当成“未安装”，也不能清空最近一次成功结果；界面保留上次状态并提供明确错误与重试。首次尚无可用清单时保持未确认。
- 只有经现有安装核对确认为当前版本精确已安装的谱面显示一键“删除”，DLC 不例外。点击后不弹确认并永久删除该自定义谱面的 `.srtb`、对应封面和全部同名可选音频；不扫描或删除安装 ZIP、既有临时文件、共享目录、相似前缀文件或 Steam 官方 DLC 文件。
- 删除先将命中的文件暂存，全部成功后提交；失败时尝试简单回滚并显示错误，若极端情况下已有部分资源无法恢复则明确提示检查安装目录，并优先尝试恢复 `.srtb`。部分恢复失败或安装内容已变化后必须重新核对实际安装状态，不能预设仍为已安装。前端允许连续删除多个谱面并为每张卡片维护独立状态，本地服务以短事务串行执行；安装请求结果尚未明确或任务活动时禁用删除，删除队列活动时禁用安装。成功后精准失效对应谱面的安装状态并同步全部／仅已安装视图、结果数、标签候选与页码，移除聚焦卡片时把焦点交给下一张卡片或结果摘要。
- 标签联想仅来自已有标签，忽略大小写；显示附加候选后剩余的结果数。
- 回车和卡片标签点击均可附加标签；已有标签可删除。删除或清空不会跳回来源卡片。
- 已选标签栏只有非空时显示，在搜索下方自然布局，滚动后吸顶；卡片点击附加保留飞行动效。
- 内容文字默认可选择、复制，包括完整标题、未折叠的短副标题与短艺人、短作者说明、难度、署名、非链接 DLC 名称、日期、统计、非控件形式的安装状态与错误，以及评论正文和评论者名称。按钮、标签、链接、评论开关及其它操作控件的文案不可选择；任何文字一旦自身承担点击操作，就只能点击而不能同时选择。可展开内容的折叠态因此只负责打开，展开后非链接正文才恢复选择，正文中的安全链接仍只负责点击。
- 歌曲标题始终完整显示并自然换行，不进入折叠卡片。副标题与艺人分别测量：自然排版不超过两行时正常完整显示且不生成浮动卡片；超过两行时各自在原位置显示一行半真实文字和末尾渐隐，折叠态只能点击、Enter 或 Space 展开。两者分别复用作者说明的锚定浮动披露行为、视觉层级、键盘与触摸规则，不能让超长字段增高所属谱面卡片；普通内容造成的合理高度差仍允许存在，不要求所有卡片绝对等高。
- 作者说明不参与搜索，卡片保持紧凑布局且不显示“描述”标题，也不使用内嵌滚动。短说明完整显示、正文可选择且不生成浮动卡片，其中安全链接只可点击；超过预览容量的长说明必须露出实际文字的标准半行，并额外保留最后 3px 渐隐余量提示全文，避免渐隐遮淡半行主体或切在段落空行。长预览在悬停、键盘聚焦或展开期间，以实体表面和文字抬升 2px、背后扩散柔光表达层次；外部命中区域不动，普通悬停不显示亮色轮廓，键盘保留边缘焦点提示，减少运动时取消位移。绝不悬停自动展开；只能点击、Enter 或 Space 让同一个说明面板保持卡片右侧原区域的顶部和左右锚点，向下长成浮动卡片。
- 作者说明、副标题和艺人的浮动卡片只增加向下高度，可以覆盖标签和卡片底部操作，但不增高谱面卡片、不下推后续卡片，也不添加可见重复标题或右上关闭键。展开后的非链接正文可选择、复制，安全链接只可点击；全文复用已加载内容，不新增服务器请求。再次点击、Enter 或 Space、按 Escape、指针移出浮动卡片时沿原路径缩回；完整收起时必须把内部滚动位置归零，下次展开从顶部开始。指针位于浮动卡片内时，普通滚轮只控制其正文；到达边界或内容不足以滚动时也不带动外页，移出后才恢复外页滚动。
- 卡片标签独立留在说明底部一行，不触发任何内容展开；浮动内容卡片可以覆盖标签，但不会改变标签的文档位置。溢出时悬浮面板展示全部标签，面板中的标签可以点击；支持键盘和触屏。
- 评论默认全部收起，但计数不依赖展开：当前整页、连续模式已渲染批次自动进入现有最多 2 并发的后台队列，优先可见或正在阅读的卡片。数量缓存与全文 DOM 独立，关闭评论不取消计数；失败保留已有数量，没有已知数量时明确显示可重试的未知状态，只有确认无评论才显示 0。已确认总数为 0 时保留统一的评论按钮外形，但禁用展开、隐藏箭头且无悬停高亮，全局展开也跳过；有条目的纯评分仍可展开。若打开后才确认 0，安全关闭并清除旧正文，焦点原在评论区域或数量按钮时回到本卡标题且不滚动页面。
- 所有谱面的手动“更新评论”共用至少 60 秒冷却，失败也计入；各按钮显示同一倒计时，由单一计时器更新。首次加载数量和打开缓存不占冷却，也不会因冷却停止；手动刷新仍通过现有队列执行，不能靠排队、换卡片或重新展开绕过间隔。前端将时间戳保存在同源 localStorage，刷新页面不会清空；这不代表不同端口或独立进程共享的后端强制限流。
- 每张卡片统计中的评论数量是按钮，点击后打开单个临时评论浮层。评论节点仍属于原卡片，呈现使用原生 `popover=manual` 顶层和固定位置，不占文档布局、不锁定页面滚动；打开、加载全文和收起都不改变页面高度或推动下方卡片。
- 宽窗浮层靠近评论按钮并限制在视口内，不留难以跨越的间隙；宽度不超过 680px 时延伸至近视口底部，上沿避开顶栏并兼顾按钮位置，保持鼠标可达。浮层内任何位置的普通滚轮都只滚动内部列表，包括标题、摘要和空白处；到顶、到底或内容较短时也不带动外层，Ctrl+滚轮和全局内嵌评论保留原有行为。轻度压暗背景与浮层深度突出阅读焦点，开合时短暂浮出与退回，阅读期间停稳，减少运动时不做空间动画。不设右上关闭键；离开来源卡片和浮层、再次点击数量按钮、Escape、外部点击或换页均可收起，必要的焦点返回不滚动页面。
- 全局“展开所有评论”开关默认关闭。开启后完整评论内嵌于各卡片，随全文延伸卡片且不随鼠标离开收起；关闭后恢复按需浮层。两种呈现只通过位置、背景和层级区分，不显示模式文字标识。完整内容始终可达，不截断、不改成摘要，也不迁入独立详情页。
- 评论时间不再显示“· Europe/Berlin”等时区尾缀，但保留原有日期换算。
- 保留下载队列、并发控制、安装状态、错误解释、目录选择和安装保护。
- Windows 覆盖安装会自动请求空闲工具优雅退出，并以原工具 PID 的进程句柄确认进程真正终止后才覆盖程序文件；`runtime.json` 消失、实例锁释放或窗口关闭都不能单独视为退出完成。存在下载或安装任务时绝不强杀工具，只允许用户等待后重试或取消。维护模式不显示自己的错误窗口，所有等待都有上限并只向安装向导返回明确退出码；文件仍占用时安装向导提供“重试／取消”，释放占用后可以原地继续，不能进入卡死或只能终止的页面。
- 用户首次提交谱面安装前，必须在专用确认卡片中看到完整的当前目录；“确认并继续”成功持久化后才允许下载，旁边的“修改目录”复用设置中的系统目录选择。关闭、按 Escape 或取消目录选择不能记忆确认或开始下载；修改目录后仍需针对最新设置版本确认。服务端同时拒绝未确认和旧目录版本的安装请求，确认跨重启保留，后续不重复提示。
- 2.0.0 是当前正式产品与设计基线；后续行为、文案或视觉规则变更必须同步更新本文件与发布记录。
- 应用只有谱面浏览这个主页面，不设常驻侧栏；官网、官方客户端和社区作为页脚补充链接。内容不足一屏时页脚必须落在应用可视底边，内容增长后随文档自然下移，不能固定悬浮或遮挡谱面。
- 尚未查询、正在加载、查询失败、没有匹配结果或结果不超过一页时，不显示没有作用的上下分页；状态提示在筛选区与页脚之间的可用内容区域居中，避免成为悬在页面上半部的孤立文字。结果区不再显示重复的安装说明；无限显示模式只在结果上方保留紧凑的显示数量选择入口，以便切回分页。
- 查询期间由结果舞台提供唯一的加载文字与旋转反馈；筛选区不重复显示加载消息，筛选按钮不旋转。难度、日期、重置和提交控件在请求完成前锁定，也不提供可反复触发请求与取消的取消按钮。接收数据的字节进度继续更新同一个中央状态。
- SpinShare Browser 是同一层级的完整名称，两部分字号、字重、颜色一致。
- 不显示重复说明页面用途的“谱面浏览”大标题；首屏直接呈现筛选，分组保留一层可见标签，统一紧凑的控件与间距。
- 默认和自定义日期状态下筛选分组都从顶部对齐；日期选择保留手输、开放区间与快捷范围，并提供清晰的年月导航。

## Brand Commitments

保留 SpinShare Browser 名称、现有标志与识别色。用户喜欢 Epic 游戏客户端的界面感觉，已同意以克制的深色层次、清晰封面和标题、统一控件及短促动效为方向，同时保留谱面工具的信息密度。

## Evidence on Hand

现有代码、用户提供的官网截图及当前界面截图、项目内测试与离线 fixture。离线预览中的示例数据必须明确为测试数据，不作为真实下载量或评价发布。已有图片不替换成生成的曲目封面。

## Product Principles

- 保留搜索、标签、评论全文可达，以及谱面安装和本地删除能力；评论呈现按本次明确授权调整。
- 把相关信息放在一起，让歌曲识别、安装和删除操作容易找到。
- 长内容可读、可达；评论收起只改变展示状态，不删减完整内容。
- 维护现有前台与后端边界，不为视觉迭代引入新的前端框架。

## 2.0.0 Release Decisions

当前发布为 2.0.1（2026-09-05），在 2.0.0 基线上修复游戏音乐试听兼容性、窗口卡死及播放器拖动区域；保持已有设置与谱面兼容。后续正式程序更新按项目级 SemVer 规则发布独立版本，不覆盖旧标签或同版本产物。

2.0.0 正式采用持久谱面数据缓存、12 小时自动同步、独立后台退避、前台“更新数据”十分钟冷却、启动聚焦卡片与托盘不打扰提示；长说明及超长副标题／艺人的半行渐隐预览与锚定原区域向下展开的浮动全文卡片、统一的内容文字选择规则、完整结果工具栏选项、取消按评论数量排序、后台评论计数、逐卡与全局展开、无模式文字标识、无时区尾缀和独立安装状态筛选均构成当前发布基线。9 月 2 日维护重发进一步确立完整目录清单刷新、单谱面定向核对、DLC 统一操作链、本地删除及安全覆盖更新为同一基线。

## Superseded Decisions

- 早期默认在卡片中完整展开评论的要求已被默认收起及全局展开开关替代。
- 先前的“数量按钮打开临时内嵌抽屉，开合高度动画推移下方卡片”方案，以及“临时展开 / 常驻展开”文字标识，均为 **superseded**。它们不能作为本轮实现依据；当前临时阅读使用不占页面布局的浮层，全局展开仍内嵌。
- 先前收起时不请求评论、打开后才获得数量的实现已被 **superseded**；当前评论数量独立后台加载和缓存，未知或失败不能显示为 0。
- 先前作者说明在卡片内滚动、通过脱离原区域的独立顶层浮层阅读全文，或在文档流内增高卡片并下推后续内容的方案已被 **superseded**；当前短说明完整显示，长说明及超过两行的副标题／艺人使用半行渐隐预览，并通过主动操作从各自原区域向下长成不改变页面布局的锚定浮动卡片，不以悬停打开。
- 先前由筛选或“刷新列表”隐式决定是否联网的实现已被 **superseded**；当前所有筛选只读本地谱面数据，超过 12 小时由独立自动通道同步，显式“更新数据”才进入前台十分钟规则。评论手动刷新的 60 秒倒计时不受此变化影响。
