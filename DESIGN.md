---
name: "SpinShare Browser"
description: "SpinShare Browser 2.0.0 正式设计系统；克制的深色谱面客户端。"
colors:
  bg: "#101114"
  sidebar: "#141519"
  card: "#1c1d22"
  surface: "#24262d"
  surface-hover: "#30323b"
  text: "#f2f3f7"
  muted: "#adb0bd"
  text-secondary: "#c8cbd5"
  text-quiet: "#9296a5"
  line: "#ffffff12"
  pink: "#eb5ba1"
  pink-soft: "#eb5ba11c"
  green: "#8bdbb0"
  error: "#ffabb9"
typography:
  brand:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "18px"
    fontWeight: 700
    lineHeight: 1.15
    letterSpacing: "-0.28px"
  title:
    fontFamily: "\"Segoe UI Variable Display\", \"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "20px"
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: "normal"
  body:
    fontFamily: "\"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.55
  reading:
    fontFamily: "\"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.68
  meta:
    fontFamily: "\"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.4
  micro:
    fontFamily: "\"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "11px"
    fontWeight: 600
    lineHeight: 1.35
  label:
    fontFamily: "\"Segoe UI Variable Text\", \"Segoe UI\", \"Microsoft YaHei UI\", \"Microsoft YaHei\", system-ui, sans-serif"
    fontSize: "13px"
    fontWeight: 600
    lineHeight: 1.4
  mono:
    fontFamily: "\"Cascadia Mono\", \"Cascadia Code\", Consolas, \"Microsoft YaHei UI\", monospace"
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.65
rounded:
  badge: "4px"
  chip: "6px"
  control: "7px"
  cover: "8px"
  search: "9px"
  surface: "12px"
  dialog: "16px"
spacing:
  "6": "6px"
  "8": "8px"
  "12": "12px"
  "14": "14px"
  "16": "16px"
  "18": "18px"
  "20": "20px"
  "24": "24px"
components:
  button-primary:
    backgroundColor: "{colors.text}"
    textColor: "#15161b"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "9px 16px"
  button-secondary:
    backgroundColor: "{colors.surface}"
    textColor: "#c9ccd7"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "9px 16px"
  install-button:
    backgroundColor: "#ecedf2"
    textColor: "#191a20"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "8px 13px"
  search-field:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    rounded: "{rounded.search}"
    padding: "5px 7px 5px 16px"
  footer-link:
    backgroundColor: "transparent"
    textColor: "{colors.muted}"
    padding: "5px 0"
  date-control:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    rounded: "{rounded.control}"
    height: "36px"
  calendar-day:
    backgroundColor: "transparent"
    textColor: "{colors.text}"
    rounded: "{rounded.chip}"
    padding: "5px 0"
    height: "36px"
  calendar-day-selected:
    backgroundColor: "{colors.pink}"
    textColor: "#22131b"
    rounded: "{rounded.chip}"
    padding: "5px 0"
    height: "36px"
  chart-tag:
    backgroundColor: "#ffffff07"
    textColor: "{colors.note}"
    rounded: "{rounded.chip}"
    padding: "4px 9px"
  chart-tag-selected:
    backgroundColor: "{colors.pink-soft}"
    textColor: "#ffc0de"
    rounded: "{rounded.chip}"
    padding: "4px 9px"
  chart-card:
    backgroundColor: "{colors.card}"
    rounded: "{rounded.surface}"
  review-toggle:
    backgroundColor: "#ffffff05"
    textColor: "#b6bac7"
    rounded: "{rounded.chip}"
    padding: "5px 8px"
  review-overlay-temporary:
    backgroundColor: "#25262e"
    textColor: "{colors.reading}"
    padding: "16px 18px"
  review-drawer-pinned:
    backgroundColor: "#ffffff02"
    textColor: "{colors.reading}"
    padding: "13px 24px 16px"
---

# Design System: SpinShare Browser

## Overview

**Creative North Star: "克制的深色谱面客户端"**

在石墨色层次上组织歌曲、作者和安装操作，用封面与标题帮助识别，用稳定的单列阅读顺序容纳长内容。沿用 SpinShare Browser 名称、既有标志和品红识别色；视觉方向来自用户认可的 Epic 类客户端感觉。

这是 SpinShare Browser 2.0.0 的正式设计基线，汇总 2026-09-01 第二轮修订（revision 2）及后续确认；本轮谱面数据缓存与更新、锚定式浮动说明卡片、完整歌曲播放器与评论规则优先于下方明确标记为 superseded 的历史方案。谱面主页面沿用 Operate 方向与原设计 seed `cc6cd6ab`，优先保证连续筛选和阅读；后续版本以此基线继续维护。

第二轮修订只保留谱面主页面，官网、官方客户端和社区移到页脚作为补充链接。首屏直接呈现紧凑、顶端对齐的筛选；SpinShare Browser 保持同一层级的完整名称。共享日历使用与外框一致的深色控件；回顶部的 DOM 和视觉位置统一在顶栏标志处，滚动后只替代图标，名称始终保留。

后续修订已纳入正式基线：评论默认收起但数量后台加载和缓存；数量按钮打开单个不占页面布局的临时浮层，“展开所有评论”则保持完整评论内嵌。两种呈现只以位置、背景和层级区分，不显示模式文字标识。评论时间去除时区尾缀，日期换算不变。结果工具栏保留独立安装状态筛选，核对本地候选集合而不额外刷新服务器谱面数据。这些变更保留原有深色方向、页面结构和安全约束。

作者说明保留紧凑卡片布局：短说明完整显示，长说明以真实文字半行裁断和末尾渐隐提示全文；主动点击或键盘确认后，同一个说明面板保持原区域的顶部与左右边界，从原预览向下长成浮动卡片。长预览的文字与实体表面轻微抬升，背后用扩散柔光表达层次；外部命中区域不动，悬停不自动打开。展开使用同一正文节点，链接和文本选择仅在展开后可用；再次操作、Escape 或指针离开时沿原路径缩回。评论标题与两种评论呈现继续保留，排序不再提供评论数量选项。

谱面数据、前台十分钟冷却和后台自动同步退避分别持久保存，正常完全退出后保留。筛选严格使用本地谱面数据；最近一次成功更新超过 12 小时后由独立自动通道同步，手动“更新数据”才进入前台冷却。启动同步、运行中同步、错误与托盘轻提示使用同一视觉语言，不以空表掩盖失败。

依据 [PRODUCT.md](PRODUCT.md)、[web/index.html](web/index.html)、[web/interface.css](web/interface.css)、[web/chart-card.js](web/chart-card.js) 与 [web/app.js](web/app.js)。前置 YAML 记录实际复用值；版式与行为以下述说明为准，不新增运行时主题层。

**Key Characteristics:**

- 石墨色分层，控件安静，功能状态清晰。
- Windows UI 字体与清晰的角色层级，歌曲识别优先。
- 单一主页面、同层级名称与直接可用的筛选，减少重复标题和导航。
- 长标题自然换行，长说明通过从原区域向下生长的锚定浮动卡片访问全文，评论按需展开且完整可达。

## Colors

中性石墨层从页面、顶栏与筛选区、卡片逐级升到控件；文字使用明亮主色与柔和次色。颜色值仅在前置 tokens 定义。

- **Primary：** `pink` 与 `pink-soft` 承担有意义的强调，绿色 `green` 表示已安装、推荐或成功。
- **Neutral：** `bg` 为页面底，`sidebar` 为顶栏与筛选区沿用的色彩 token 名称，并不表示存在侧栏；`card` 为谱面，`surface` / `surface-hover` 为控件及其交互状态。
- **Text：** `text` 用于主信息，`text-secondary` 用于长说明和评论正文，`muted` 用于辅助信息，`text-quiet` 仅用于时间码、路径和最低优先级状态。低透明度 `line` 分隔卡片内部区域。
- **Feedback：** `error` 配合具体错误文案；不要只靠颜色解释安装或请求状态。

**The 功能强调 Rule.** 品红用于选中、焦点、进度及现有链接、难度和提示状态；不铺成装饰性大色块。主要筛选与安装按钮使用浅色底。

## Typography

界面正文优先使用 `Segoe UI Variable Text`，大标题和品牌优先使用同系列 Display 光学版本；Windows 10 回退到 Segoe UI，中文依次回退到 Microsoft YaHei UI / Microsoft YaHei。字体全部来自系统，无 Google Fonts、下载字体、授权或加载抖动。路径使用 Cascadia Mono / Cascadia Code / Consolas，并保留 Microsoft YaHei UI 中文回退。

- `brand` 用于完整名称 SpinShare Browser；两词始终使用相同字号、字重和颜色，视口不超过（1000px / 680px）时整体降为（17px / 16px）。页面的“谱面浏览”一级标题仅供辅助技术读取，不作为可见的大标题。
- `title` 是歌曲标题；卡片内容宽度不超过（460px）时降为（18px）。用户生成的中英日混排标题保持正常字距，只有固定拉丁品牌名使用轻微负字距。
- `body` 是全局默认；`reading` 用于说明和评论，正文最大行宽（72ch）。两者均为（14px），依靠行高、字重和颜色区分角色。
- `label` 用于按钮、选择器当前值与关键控件（13px / 600）；`meta` 用于日期、统计、作者、页脚和字段名（12px / 400）；`micro` 只用于难度短码、时间码和紧凑徽章（11px / 600）。普通界面文字不得降到（8–10px），窄窗优先换行或隐藏低优先信息。
- 系统提示、帮助、状态、空态与错误正文使用（13px / 400）；只有阻塞流程的主提示使用（14px / 400），提示标题才使用 600 或 700。字段、统计和其它真正的元信息继续使用 12px，不能用同一小字号承载正文与元数据。
- 字重限制为系统稳定支持的（400 / 600 / 700）；正文与普通元信息用 400，控件强调用 600，品牌和主要标题用 700，避免中英文 fallback 粗细失衡。
- 日期、难度与统计使用等宽数字；保留用户内容换行和任意长词回流，不改写为界面翻译文本。
- 程序生成的提示、帮助、状态、空态与错误文案不使用句末句号；多句说明保留必要的句内分隔，进行中状态统一使用单个省略号字符（…）。文件扩展名、版本号、网址以及用户生成的标题、说明和评论不受此规则改写。

**The 文字完整 Rule.** 标题与艺人自然换行，说明和评论保留全文；长说明仅在卡片预览处半行裁断，全文通过从同一区域向下展开的浮动卡片访问，不改成摘要。

## Layout

页面只有一个谱面主页面和一个主文档滚动区，不设常驻侧栏。桌面固定顶栏（64px）；主体居中，最大宽度（1640px），默认内边距为上（24px）、两侧（36px），底部留白由页脚自身承担。首屏不插入可见的“谱面浏览”大标题，直接进入筛选。筛选、搜索、结果工具、已选标签和分页沿文档顺序排列；筛选区可收起，标签非空时才显示并吸顶在顶栏下。官网、官方客户端和社区位于内容末尾页脚，不作为应用内页面导航。

筛选分组仅保留一层可见标签；fieldset 的 legend 保留为 sr-only 语义。难度与日期分组均从顶部对齐，展开自定义日期也不改变对齐方式。宽屏中主筛选按钮与输入控件同一行，辅助状态文字可以另起行；缩窄到双列或单列时，操作区按下面的断点换行。

当前浏览页使用单列横卡；卡片内左边为封面、标题、副标题、艺人、难度和作者，右边为说明及一行标签，统计与安装操作位于底部。评论节点仍保留在卡片 DOM 内；默认临时阅读以顶层浮层呈现，不占卡片布局，只有全局展开时完整评论接在操作区之后。默认封面为（112px）方形且使用 cover 裁切，封面上只叠放歌曲播放按钮，不承担官网跳转；标题是可选择、复制的普通文本，标题旁独立的外链小图标才打开官网谱面页，默认低亮度，卡片悬停或内部聚焦时变清晰。标题区内边距（20px 24px 18px），左右内容比例（0.95:1）、间距（28px）。无说明和标签时，歌曲区占满；不要预留空的说明列。

歌曲信息内部采用网格：封面跨标题 / 艺人和难度两行，文字位于右列，署名放在下一行并横跨整个歌曲信息区。标题继续自然换行，不裁切文字，也不把署名挤在封面右侧而留下大块空白。统计区分为指标组与操作组；安装状态和下载按钮始终属于同一操作行并垂直居中对齐。日期参与指标组的自然换行，不强制独占一行。

| 响应条件 | 实际变化 |
| --- | --- |
| 视口 ≥1900px | 主体两侧（48px）；封面维持默认尺寸；标题区比例（0.8:1）、间距（40px）。 |
| 视口 ≤1100px | 主体两侧（26px）；筛选改两列、操作独立一行，分页重新分行。 |
| 视口 ≤1000px | 主体两侧（22px）；完整名称整体缩小，结果工具间距收紧。 |
| 卡片内容宽 ≤700px | 歌曲与说明上下排列，分隔线从左侧移到上方；卡片横向内边距（20px）。隐藏统计文字标签，保留数字、图标与按钮可访问名称。 |
| 卡片内容宽 ≤560px | 统计指标和操作组明确分成两行；安装状态和下载按钮保持同一行、垂直居中对齐。 |
| 卡片内容宽 ≤460px | 封面（92px），卡片横向内边距（16px）；歌曲信息网格收紧，署名仍占全宽，评论作者与正文上下排列。 |
| 视口 ≤680px | 顶栏（58px），主体两侧（14px）；筛选和分页单列，页脚分行，原生窗口按钮缩窄；语言仍可在设置中选择。 |

卡片宽度使用容器查询，不能把（700px / 560px / 460px）误作视口断点。页面不应横向滚动；长标签在专属浮层换行，长目录和错误消息可断行。视口（900px）另用于 JavaScript 分页按钮密度，不改变卡片布局。

## Elevation & Depth

主体以色阶和细分隔线表达层次，不使用玻璃背景或整卡投影。标签选择、完整标签、帮助浮层和日期日历使用共用抬升表面；长描述在悬停时抬升右侧预览表面，展开后同一表面从原锚点向下形成浮动卡片，临时评论浮层使用轻度背景压暗集中阅读注意力，设置和确认对话框使用更深阴影与暗化背景。回顶部留在顶栏，不使用单独的悬浮阴影。具体阴影与运动参数记录在 `.impeccable/design.json`，不添加前置 token 组。

**The 静态分层 Rule.** 整张谱面卡片不靠阴影或悬停位移强调层级；长说明预览仅抬升内部表面，外部命中区域不动，浮层和对话框使用阴影表达覆盖关系。

吸顶标签为（25），顶栏为（30），底部安装队列为（40）；回顶部（35）位于顶栏内部层叠上下文。评论、标签等原生 popover 及 dialog 进入浏览器顶层，避免被卡片裁剪；描述浮动卡片仍锚定在所属谱面卡片的说明区域，通过所属卡片内的覆盖层级遮住标签或底部操作，不改变文档流几何。不要单靠提高 z-index 解决功能控件互相覆盖。

## Shapes

小圆角控件、略大的内容容器与圆形头像共同构成现有形态。前置 `rounded` 区分徽标、标签、控件、封面、搜索、表面和对话框。封面与回顶部使用同级圆角，窄卡封面使用控件级圆角，日历日期使用标签级圆角；头像保持圆形。复用细描边 SVG 图标，不引入图标字体。

## Components

### Buttons and inputs

主筛选按钮与安装按钮是浅底深字；次按钮采用控件底色。普通按钮最小高度（36px），安装按钮（34px）；搜索框默认（48px），窄窗（44px）。悬停只改变颜色；禁用态使用稳定的灰阶前景与背景，不降低整段文字透明度或造成发灰锯齿；不增加悬停放大。

键盘焦点使用柔和品红外轮廓（1px，通常偏移 2px）；搜索框及正在编辑的文本字段使用同等细边框。指针点击完成后不保留焦点圈或悬停式高亮，只有 Tab / Shift+Tab 切回键盘导航时显示焦点；选中态仍由底色、勾选或箭头独立表达。已有原生输入、details 和 dialog 语义需要保留，强制颜色模式用系统颜色补充边框与选中状态。

选择器以原生 `select` 作为唯一数据源，显示层统一增强为按钮和 `listbox` 浮层，避免 Windows 原生展开层的白边、蓝色选中行与不可控字形。浮层复用日期面板的深色表面、圆角、阴影和短距离淡入；当前项用轻量品红底与勾选表示。方向键、Home / End、首字母检索、Enter / Space、Escape、Tab、外部点击、禁用同步与中英文即时更新都必须可用。增强失败前不得隐藏原生控件。

结果工具栏中的安装状态、排序依据和排序方向采用完整的键值字段：字段名在上方使用（12px / 400）的低优先颜色，当前值在下方使用（13px / 600）的实体按钮。每个字段作为整体换行；安装筛选与排序组之间用低对比分隔线建立关系，不再把键和值排成同一层级的连续文字。

### Catalog synchronization

筛选、文字搜索、标签、排序和分页只使用已加载的本地谱面数据，绝不因这些动作触发服务器更新。`GET /v1/charts` 只读保存的数据与状态；前台手动“更新数据”和后台自动同步使用独立动作。手动按钮旁放一个小型问号帮助浮层，说明本地谱面数据超过 12 小时后会自动同步，多数情况下无需手动操作；按钮和帮助控件都不使用原生 `title` 小字。问号在鼠标悬停或键盘聚焦时显示内容，移开或失焦即关闭，不依赖点击；浮层朝不会遮挡相邻按钮的方向展开。今后新增的问号帮助都沿用这套悬停与聚焦规则。

谱面数据、前台 `lastAttemptAt` / `fetchedAt` 与后台失败次数、错误和下次允许时间保存在应用状态目录的 `charts-cache.json`，默认目录为 `%LOCALAPPDATA%\SpinShareBrowser`，与程序安装目录和谱面目标目录独立，正常完全退出不删除。前台服务在联网前先原样原子写现有状态做本地预检；完成 DNS、TCP 与 TLS 连接后、发送谱面数据请求前，再原子保存本次请求及至少十分钟冷却，冷却无法保存时不得发送请求。明确的 401／403 访问拒绝只有在成功恢复原状态后才立即重试；限流、服务器异常、等待响应超时、请求或正文中断、响应过大／无效及最终数据提交失败都保留冷却。只有确认请求未到达服务器的连接阶段失败、本机预检失败，以及安全回滚的访问拒绝不写入新前台冷却。旧缓存格式保守迁移，不能通过升级或重启缩短已有冷却。新谱面数据先原子写入，成功后才发布。

没有本地谱面数据或最近一次成功 `fetchedAt` 已超过 12 小时时，启动界面先显示中央聚焦同步卡片，背景轻度压暗，工具内容暂不可操作。卡片只显示真实连接、接收和保存阶段；有总长度时显示确定进度，没有时显示不定进度，并始终可显示真实已接收 MB，不伪造百分比。成功后用短确认态自动退场。失败且有旧谱面数据时显示“重试”与“使用本地数据”，没有本地谱面数据时显示“重试”与“退出”；不设 X 或跳过入口。

自动通道不写前台十分钟冷却，失败使用独立持久退避（5 分钟、15 分钟、1 小时、6 小时）。自动退避只限制自动任务，用户明确点击重试即进入前台规则；已有前台冷却时自动任务不能绕过它。运行时达到 12 小时以单任务、低优先级更新，旧谱面数据在原子发布前持续可读，筛选和安装不被阻塞。托盘且无前台窗口时，实际更新或失败使用右下角无边框、无激活、不进任务栏的自有轻提示；成功但数据未变化保持安静。全屏、省电或按流量计费环境延后，提示固定在工作区右下角原地淡入、停留后原地淡出，不抢焦点。

错误必须区分网络、访问拒绝、限流、服务器、传输、格式／大小与本地存储故障。失败反馈采用结构化卡片：标题给出当前结论或可执行动作，正文使用清晰的键值行呈现重试状态、可重试时间与当前使用的数据；不得把传输阶段、内部请求名称或服务器资源成本直接拼成面向用户的长句。取得新谱面数据后失效相关派生缓存并重新计算结果和候选计数；谱面数据更新本身保留标签、关键词及其它搜索选择。前台数据更新间隔、后台退避与评论手动刷新的 60 秒倒计时分别处理。

### Shared date calendar

起止日期使用同一个自制深色日历，复用原生 popover 和 button，不依赖浏览器日期选择面板或第三方日期库。手输格式为 YYYY-MM-DD；任一端点可以留空作为开放范围，快捷日期范围继续可用。合法日期下界为（2020-01-01），上界为 Europe/Berlin 的今日，不随本机时区改成另一个日期。

日历顶端可直接选择年份与月份，也可使用上一月、下一月按钮。日期按周一开始的七列排列；当前端点使用实色品红，有效闭合范围使用淡品红，今日使用细内描边，越界日期禁用。年月和星期名称随界面语言显示，日期按钮有完整的可访问名称；日历宽度随可视区域收缩，位置避开固定顶栏，锚点滚出可视区域时关闭。

日期网格采用一个可 Tab 到达的日期焦点：左右箭头移动一天，上下箭头移动一周，Home / End 移到本周首日 / 末日，PageUp / PageDown 翻月，配合 Shift 翻年，Enter / Space 选择。Escape 关闭并返回当前端点的日历按钮；选择日期或清空当前端点后同样返回按钮，不强制滚动。年份、月份及其它按钮仍遵循原生键盘行为。

共享 popover 在 DOM 中始终紧跟当前端点的 date-control；切换起止端点时移动同一个节点，不将它固定挂在文档末尾。这样浮层即使进入浏览器顶层，Tab 顺序仍接在当前日期控件之后。日历只更新筛选条件，不自行触发查询。

### Chart cards and reviews

卡片不设固定总高度，作者说明仍位于原有紧凑信息区，不显示可见“描述”标题，也不参与搜索。移除说明内嵌滚动：短说明完整显示，没有光圈或展开交互，原生 HTTP(S) 链接照常可用；长说明超过预览容量时必须在真实文字行中间露出半行并以末尾渐隐提示全文。预览容量按五行范围测量（8.5em，行高 1.7），裁断优先取该范围内最后一条实际文字行的几何中点，无法测量时回退到四行半（7.65em）；容器在中点后额外保留 3px，渐隐只占末端，不让半行退化成几个像素。不能落在段落空行，也不能只做整行截断。

长预览在悬停、键盘聚焦或打开期间，以实体表面和文字一起抬升（2px）表达可操作性；外部命中区域保持原位，后方两层扩散柔光向下（1px），不以亮色描边代替背景光晕。实体表面使用（linear-gradient(150deg, #2b2a36, #24252d)）和静态落影（0 8px 16px #0007、0 2px 5px #0004），背后光晕使用（0 0 18px 3px #af86df40、0 9px 30px 2px #807bd938）。仅透明度与 transform 在（180ms）内过渡，不动画化阴影，也不持续循环；减少运动时取消位移。键盘通过表面边缘（#c7b1edb3）提示焦点，强制颜色模式使用 Highlight，普通悬停不显示亮色轮廓。

悬停绝不打开全文，只有点击、Enter 或 Space 才让同一个说明面板从原区域向下展开；再次点击或按键、Escape、指针移出浮动卡片时沿原路径收起。长预览内部内容为 inert，不可选择、复制或点击链接；展开后同一正文节点恢复文本选择和安全链接，点击链接或已有文本选择时不能误收起。标签保持独立底部一行和原有文档位置，浮动说明可以覆盖它，但标签交互不触发描述；不能把说明区和标签包装成同一个展开触发器。

描述全文只复用已加载的数据与既有安全链接渲染，不新增服务器请求，也不摘要化内容；展开后保持原预览的顶部、左侧与右侧锚点，不显示歌名、标题、额外头部或右上关闭键，只向下增加高度。浮动卡片可以覆盖标签和底部安装操作，但不得增高所属谱面卡片、推动后续卡片或创建背景幕。指针位于浮动卡片内时，普通滚轮只控制说明正文；到顶、到底或内容不足以滚动时也不传给外页，用户移出卡片后才恢复外页滚动。打开另一条描述、临时评论或标签浮层时，先收起当前描述以保持单一阅读焦点。

评论默认收起，卡片统计中的评论数量使用轻背景、细边框的按钮，带随展开状态转向的 chevron。按钮留在指标组的正常布局中，不用负 margin 补偿位置。当前整页、连续模式已渲染批次自动进入现有最多 2 并发的评论队列，优先可见或正在展开的卡片；计数与全文 DOM 分开维护，收起不会取消数量加载。刷新失败保留已有数量，否则显示可重试的未知状态；未获取和失败不能伪装为 0。

有效成功响应仅在缺少 `reviews` 且 `average` 严格为 `false` 时兼容为空集合，其它损坏响应仍报错；已确认总数为 0 时保留统一评论按钮外形，但禁用展开、隐藏箭头且无悬停高亮，全局展开也跳过，有条目的纯评分仍可展开。若打开后才确认 0，立即安全关闭并清除旧正文；焦点原在评论区域或数量按钮时回到本卡标题，并使用 `preventScroll`。

所有谱面的手动“更新评论”共用至少（60 秒）冷却，失败也计入。一个全局计时器同步按钮的禁用状态与剩余秒数，不能为每张卡片分别启动倒计时。首次计数和打开缓存不消耗手动刷新额度；冷却、现有队列、进行中的读取及缓存状态分开处理，不靠换卡片、开合或排队绕过间隔，也不因冷却中断后台计数与缓存阅读。时间戳保存在前端同源 localStorage，页面刷新后恢复剩余时间；范围限于共享该源存储的页面，不承诺不同端口、隔离存储或独立进程间的后端强制限流。

点击数量按钮打开单个临时评论浮层。评论节点仍在所属卡片的 DOM 中，使用原生 `popover=manual` 进入浏览器顶层并固定定位，不锁定 body，也不改变文档高度。宽窗以评论按钮为锚点，根据可用空间选择上方或下方，宽度最多（680px）、高度最多（560px），并限制在视口内，不留难以跨越的间隙。视口宽度不超过（680px）时浮层延伸至距底部（12px），上沿兼顾按钮位置、视口高度与固定顶栏，保持从按钮到浮层的短路径。浮层头部只保留曲目上下文，不设右上关闭键；透明外壳以（3px 0）内边距容纳动效，底色（#25262e）、圆角（12px）和阴影（0 16px 44px #0009）落在内部表面上，背景幕（#05060a2e）轻度压暗且不拦截指针事件。浮层内任何位置的普通滚轮都只控制内部评论列表，包括标题、摘要、空白与正文；到顶、到底或短内容无需滚动时，也不把滚轮传给外层，列表保留 `overscroll-behavior: contain`，不推移下方卡片；Ctrl+滚轮及全局内嵌评论仍放行默认行为。

鼠标离开来源卡片和浮层后柔和退回；再次点击数量按钮、Escape、外部点击、换页或来源卡片退出当前结果也会关闭。卡片与浮层之间移动不得提前收起。键盘和触摸均可阅读与关闭；需要返回评论按钮时使用 `preventScroll`，不让焦点恢复引发滚动。临时浮层的打开、异步全文到达和收起都不得触发页面高度变化或下方卡片位移。

全局“展开所有评论”默认关闭。开启后关闭临时浮层，并在每张卡片内展示完整评论，随内容延伸卡片，不随鼠标离开收起；关闭后恢复按需浮层。浮层通过覆盖层级和背景表达临时阅读，全局模式通过卡片内的位置与背景表达持续阅读，不显示模式文字标识。全局模式下数量按钮表达由开关保持的展开状态，不暗示仍可逐卡关闭。

两种呈现均保留完整评论和仅评分项，不使用摘要或条数截断。限制浮层视口高度只用于内部滚动，不限制内容；全局内嵌评论不设固定总高度。缓存数量和响应可以在收起时更新，正文仅在需要展示时构建；分批构建不是内容上限。未加载、失败与确认无评论分别使用简短状态文字。评论时间不显示“· Europe/Berlin”等时区尾缀，原有日期换算保留。

### Tags and search

首次有效筛选前搜索与添加标签不可用。文本搜索与标签独立，多个已选标签按 AND 组合，改变其他筛选不清除标签。联想只允许现有标签、忽略大小写，并显示叠加当前搜索和已选标签后的结果数；回车与卡片标签点击均可添加。

卡片标签固定一行，溢出时 hover、键盘 focus 或触屏打开完整标签浮层，浮层按钮仍可添加。已选栏自然布局后吸顶；超出一行的已选项通过计数按钮访问。添加卡片标签保持来源位置并飞入筛选栏；删除或清空保存当前视口、页码，焦点移动不触发滚动。

### Installation status filter

结果工具栏中的安装状态默认“全部”，可选择“仅已安装”或“仅未安装”；首次有效筛选前不可用。它与难度、日期、关键词搜索范围、AND 标签及排序共同决定结果，改变任一条件不重置其它条件。安装状态、排序依据和排序方向共享同一键值字段与选择浮层，不另造互不一致的视觉样式。

过滤对象是整个候选集合，不只是当前页；安装状态通过本地 `/v1/installations/check` 核对，不触发额外的服务器谱面数据更新。未知、正在核对或核对失败必须与已确认的“未安装”区别表达，不能把缺少确认当成未安装，也不能用成功样式掩盖核对失败。

### Header, footer and back to top

顶栏保留原有标志、同层级的 SpinShare Browser 名称、语言与设置，以及桌面窗口控制；中部容纳唯一的全局播放器。官网、官方客户端和社区只作为页脚补充链接，使用柔和文字和小图标，悬停变亮；不再提供侧栏的当前页选中态。

主体与页脚共同占满顶栏以下的动态视口。内容不足一屏时，弹性剩余空间只能出现在主内容与页脚之间，页脚底边贴住应用底部；内容超过一屏时，页脚保持普通文档流并位于结果之后，不能固定覆盖分页、谱面或安装进度。宽桌面将补充链接、安装目录和署名整理为一行；中等宽度把安装目录放到第二行，窄窗口再按链接、目录、署名顺序单列。常态底部余量为（24px），下载活动出现时才扩展为（120px）给浮动任务条让位。

空状态由结果舞台吸收筛选器与页脚之间的剩余高度，提示在该舞台内水平和垂直居中。未查询、加载、失败、零结果以及不超过一页的普通结果都隐藏上下分页；只有确实存在第二页时才显示完整的上下分页。无限显示模式仅在结果上方保留紧凑的显示数量选择入口，以便切回分页。结果区不显示重复的安装说明。空状态不能通过固定分页、固定提示或人为添加页面底部空块来定位。

本地筛选通常立即完成，不再复制谱面数据联网的加载反馈。启动自动同步由顶层聚焦卡片独占反馈，手动“更新数据”与运行时自动同步使用紧凑状态或轻提示；同一阶段只能有一套可见反馈。同步期间提交按钮保留原位并禁用，不附加第二个旋转标记，也不提供会造成重复提交的取消入口。顶栏播放器与无需写入谱面数据的功能不受影响。

所有窗口宽度的回顶部都位于顶栏标志原位，按钮 DOM 也在 header 内。出现阈值为滚动距离大于 `max(300px, 0.5 × 视口高度)`，隐藏时不可聚焦。滚动后只淡出标志图像，以回顶部按钮替代；SpinShare Browser 名称始终保留且不分出次级样式，回顶后标志恢复。

桌面按钮为（42px）方形，位置为（left 20px、top 11px）；视口不超过（680px）时为（40px）方形，位置为（left 7px、top 9px）。它不占用卡片操作区域。点击将焦点交还筛选摘要并滚到页首，减少运动时直接滚动。

### Global audio player

页面只使用一个顶栏播放器和一个媒体元素。卡片封面上的覆盖按钮选择或切换曲目，不在每张卡片内复制播放器；歌名和封面都不再作为官网链接，官网入口由标题旁低调的独立外链图标承担。没有当前曲目时播放器隐藏，封面悬停只显示按钮，绝不推断或自动开始播放。

顶栏播放器以较大封面作为唯一播放 / 暂停按钮，右侧第一行并列歌名与艺人，第二行显示当前时间、可拖动进度和总时间；不放难度标签或其它谱面元数据。播放器使用谱面包中的完整歌曲音频，进度范围取媒体实际总时长；抵达真实终点进入结束态，从终点拖回范围内可由该位置继续。首次点击播放时会提示空格暂停 / 继续以及左右方向键后退 / 快进（5 秒），该提示在本机持久记录。

Space 在已有当前曲目时全局切换播放 / 暂停；输入框、文本域、选择器、按钮、summary、可编辑内容、具有原生 Space 行为的 ARIA 控件和阅读浮层优先。组合输入与 Ctrl / Alt / Meta 修饰均让出快捷键；按键重复不重复切换。没有当前曲目时不阻止默认行为，也不把当前悬停卡片当作待播放曲目。快捷键确实暂停时立即在顶栏封面显示新的控制状态，恢复则等媒体实际进入 `playing` 后才显示；反馈保持（900ms）再淡出。加载取消、播放拒绝或其它未真正改变状态的路径不显示，并移除进度条因这次全局快捷键产生的焦点框。

音频 URL 只能由严格匹配 `spinshare_[a-f0-9]{1,64}` 的 `fileReference` 拼接到 `https://spinshare.b-cdn.net/uploads/audio/` 下的 `_0.ogg` 或 `_0.mp3`；播放流程不访问曲目详情、下载或其它计数接口。每次新选曲先尝试 OGG，媒体错误或（15 秒）内未就绪时只回退一次 MP3；MP3 同样只有（15 秒）加载边界，失败后显示可重试状态，不形成格式循环。回退保留有效进度，不做人为时长截断。

筛选、文字搜索、标签、排序、分页和连续加载保留当前曲目及进度；它不依赖当前卡片仍在 DOM 中。谱面数据发布新副本后核对曲目 ID、`fileReference` 与 `updateHash`，缺失或变化时立即释放旧源。文档隐藏、桌面窗口最小化或进入托盘时暂停，重新可见不自动播放；页面卸载或应用退出时清除源、媒体监听、加载计时器与动画帧。

首次出现、换曲和播放状态只使用既有（150 / 180 / 220ms）节奏。顶栏保存两个封面槽位：新封面以轻微缩放淡入，旧封面同时淡出，加载失败再平滑回到占位图，避免先清空再闪现；所有旧回调以曲目 token 失效。空格状态反馈只在 48px 封面内使用一次短促的透明度和缩放，不改变播放器或页面几何。减少运动时保留状态与透明度表达，不做空间位移、缩放或持续运动。

### Installation, dialogs and motion

安装状态、错误说明和细进度条留在卡片，正在处理的任务另有底部队列；完成态使用绿色。首次点击安装或更新谱面时，使用一个紧凑的顶层确认卡片完整显示当前目录；“当前谱面安装目录”是唯一标题，路径紧接其后，“修改目录”和“确认并继续”相邻，不再叠加图标或重复说明。关闭、Escape 与系统目录选择取消均不提交下载；修改目录后卡片原地更新路径并等待对最新 revision 再确认。确认成功才持久记录并继续原谱面，之后跨重启不再提示；本地服务仍对未确认或旧 revision 的安装请求硬拦截。设置、目录选择、窗口控制和退出确认沿用原生服务与既有保护，不能以视觉重做绕过它们。

交互动效使用同一节奏标尺：颜色、淡出和直接反馈为（150ms），短距离位移、开关与折叠提示为（180ms），卡片和浮层进入为（220ms）；只有表达标签移动路径的飞行动效使用（280ms）。大型筛选器的文档布局使用原生 `details` 即时切换，不逐帧动画高度；只让摘要箭头和新显示的内部控件以（180ms）透明度 / 短位移提示状态。描述浮动卡片的顶部与左右锚点全程不动，只让同一说明节点向下增高或沿原路径缩回：几何读取集中在状态写入前，展开约（220ms）、收起约（180ms），正文仅叠加透明度和（3px）以内位移，结束后移除内联高度与 `will-change`。快速反向操作从当前可见高度连续反转，不停顿或跳变。临时评论浮层保持（220ms）进入 /（150ms）退出，阅读期间停稳。减少运动时所有开合直接到最终状态。播放器复用同一节奏，换曲时以双封面槽位交叉淡化，不先隐藏旧图造成闪白，也不增加持续漂浮。标签飞行为（280ms），并接收（220ms）落点提示。加载旋转保留独立的线性循环速度，不纳入交互时长标尺。仅在内容首次出现或操作反馈时使用；减少运动、页面隐藏或宿主不可见时停用 / 结束运动。无需新增动画库。

## Do's and Don'ts

### Do:

- Do 沿用统一的色彩、排版字阶、圆角与 Windows UI 字体栈；新文案保持中英文可用。
- Do 保持单一谱面主页面、同层级的完整名称、紧凑且顶端对齐的筛选；外部入口留在页脚。
- Do 保留日期手输、直接年月选择、完整键盘导航和开放范围；共享日历跟随当前端点的 DOM 顺序。
- Do 保留完整评论、独立 AND 标签、候选结果数和键盘可达的全部标签弹层；删除标签保持视口和页码。
- Do 让长说明在实际文字半行处裁断并渐隐，悬停或聚焦以实体表面抬升和背景柔光表达层次，外部命中区域不动；点击或 Enter / Space 才让同一份无可见标题的全文保持原顶部与左右锚点向下长成浮动卡片，再次操作、Escape 或指针移出时沿原路径缩回，短说明与其链接保持普通内容行为，标签独立。
- Do 在评论收起时仍后台加载数量，独立缓存；失败保留已知数量，没有数量时明确显示可重试的未知状态；已确认零条保留按钮外形但禁用展开、隐藏箭头与悬停高亮，全局展开跳过，有条目的纯评分仍可展开。
- Do 让所有谱面共用 60 秒手动评论刷新冷却，失败也计入；用单一计时器和同源 localStorage 时间戳同步倒计时，不影响首次计数或缓存阅读。
- Do 用位置、轻度背景压暗和层级区分单个临时浮层与全局内嵌评论；默认收起，全文和键盘关闭均可达，不设右上关闭键。
- Do 保持临时评论浮层和描述浮动卡片开合前后的页面高度、所属卡片及后续卡片位置不变；描述从原说明区域向下覆盖标签或底部操作，普通滚轮只滚动内部，到边界或短内容也不带动外层。评论宽窗靠近按钮，窄窗延伸至近底部且保持按钮可达，阅读期间保持静止。
- Do 让安装状态与其它条件独立组合，核对整个候选集合，并明确区分未知、核对失败与未安装。
- Do 让长内容自然回流，在宽桌面、窄桌面和窄窗检查安装按钮、设置与窗口控制均可操作。
- Do 让歌曲署名占信息区全宽；窄卡的统计与操作分行，但安装状态和下载按钮保持同一行对齐，不裁切标题换取空间。
- Do 只让封面按钮选择全局播放曲目，保留歌名的文本选择与复制，并用独立低调的外链图标打开官网；筛选、排序和翻页不打断有效播放。
- Do 用可见状态和 `aria-label` / `aria-description` 说明图标控件，不生成 HTML `title` 原生悬停小字；用户已经指定的标签、描述和评论浮层继续保留。
- Do 保留持久谱面数据缓存、前台十分钟冷却、后台自动退避、评论刷新倒计时、安装队列、并发限制、错误解释及安装目录保护；筛选只读本地谱面数据，12 小时自动同步与手动“更新数据”分开，真实故障与首次无数据明确区分。

### Don't:

- Don't 引入前端框架、远程字体或静态资源路由；保持现有 Python / WebView2 与内联页面组装。
- Don't 自动请求会增加官网浏览量的曲目详情接口；不要让已经发送或可能到达服务器的谱面数据请求、重试按钮或进程重启绕过十分钟冷却，也不要让确认未到达服务器的连接阶段失败、本机预检失败或已安全回滚的访问拒绝占用冷却。
- Don't 为每张卡片创建媒体元素、让歌名或封面继续跳转官网、从不可信字段接受完整音频 URL，或在无当前曲目时让 Space 播放悬停项。
- Don't 让按钮、图标、徽章、日期、统计、头像或截断文字通过鼠标停留弹出原生小字提示。
- Don't 截断或摘要化评论，或将其迁入独立详情页；评论节点保留卡片 DOM 归属，临时阅读可进入浏览器顶层。
- Don't 用 0 代替未知或失败的评论计数，也不要因收起正文取消后台计数。
- Don't 再用临时内嵌抽屉或高度动画推移下方卡片，不锁定页面滚动，也不添加模式文字标识。
- Don't 为切换安装状态额外更新服务器谱面数据，或将未知、核对失败当成未安装。
- Don't 用数据更新冷却倒计时或禁用状态阻碍本地筛选，不得让后台通道绕过前台冷却、发布尚未成功落盘的新谱面数据，或把首次没有可用缓存的失败显示为空表。
- Don't 重新加入常驻侧栏、可见的“谱面浏览”大标题或重复分组标签；不要把 Browser 变成次级名称。
- Don't 搜索作者说明，清空其它筛选时连带清空标签，或删除标签后跳回原卡片。
- Don't 让描述预览内嵌滚动、悬停自动打开全文、在空行裁断，或让长预览的 inert 内容抢占选择和链接操作；展开时不要改变谱面卡片高度、推动后续卡片、泄漏普通滚轮到外页，也不要添加可见标题或关闭按钮；不要恢复按评论数量排序。
- Don't 让回顶部覆盖安装操作，替换既有标志，或把离线假封面、示例统计当作生产内容。
- Don't 让后续局部改动绕过本设计基线、产品约束或发布记录。

生产结构仍是 `web/index.html`、`web/interface.css`、`web/chart-card.js`、`web/app.js` 四个源码文件，由 `src/spinshare_portable.py` 按固定白名单拼为单个内联 style / script 页面；语言目录仍为 `web/locales.json`。源码拆分不意味着新增静态服务入口。

设计面板 sidecar 中的组件文字仅为样式示例，衍生色带仅用于色板展示；两者都不是生产内容或新增生产色值。未把未使用的 `--card-hover`、离线预览封面、测试统计和本地会话数据提升为设计规范；2.0.0 的生产界面与本文件共同构成正式视觉基线。

## Superseded history

以下记录保留设计演进，但不再定义当前谱面数据更新、说明与临时评论交互：

- **Superseded：早期完整展开。** 评论曾默认全部内嵌展开；现由默认收起和全局“展开所有评论”控制。
- **Superseded：临时内嵌抽屉。** 数量按钮曾在当前卡片内打开抽屉，使用展开（200ms）/收起（150ms）的高度和透明度动画推动下方卡片，离开整张卡片后收起。本轮改为单个原生顶层固定浮层，开合和正文加载不改变页面高度。
- **Superseded：模式文字。** 先前头部显示“临时展开 / 常驻展开”；当前不显示这些标识，只通过位置、背景和层级区分。全局展开继续内嵌，此行为未被废弃。
- **Superseded：展开后才取数量。** 先前收起时不发起评论读取；当前整页或已渲染批次独立后台加载和缓存数量，关闭正文不取消计数，失败不能当作零评论。
- **Superseded：说明内嵌滚动、脱离原区域的顶层浮层与文档流增高。** 说明先后采用过卡片内滚动、独立居中全文浮层，以及在文档流内增高卡片并下推后续内容；现由短说明完整显示、长说明半行渐隐预览和锚定原区域向下生长的浮动卡片替代。悬停不自动打开，再次点击、Escape 或指针移出即可沿原路径收起。
- **Superseded：筛选隐式联网。** 先前筛选或“刷新列表”自动决定是否更新谱面数据；当前筛选严格读取本地谱面数据，超过 12 小时由自动通道同步，显式“更新数据”才使用前台十分钟冷却，评论手动刷新的 60 秒倒计时不变。
