---
name: "SpinShare Browser"
description: "当前可交互原型的设计系统；克制深色谱面客户端，待用户视觉审核。"
colors:
  bg: "#101114"
  sidebar: "#141519"
  card: "#1c1d22"
  surface: "#24262d"
  surface-hover: "#30323b"
  text: "#f2f3f7"
  muted: "#a4a7b5"
  line: "#ffffff12"
  pink: "#eb5ba1"
  pink-soft: "#eb5ba11c"
  green: "#8bdbb0"
  reading: "#c4c7d2"
  note: "#bec1ce"
  error: "#ffabb9"
typography:
  brand:
    fontFamily: "\"Segoe UI\", \"Microsoft YaHei\", sans-serif"
    fontSize: "17px"
    fontWeight: 650
    lineHeight: 1.15
    letterSpacing: "-0.35px"
  title:
    fontFamily: "\"Segoe UI\", \"Microsoft YaHei\", sans-serif"
    fontSize: "20px"
    fontWeight: 600
    lineHeight: 1.35
    letterSpacing: "-0.25px"
  body:
    fontFamily: "\"Segoe UI\", \"Microsoft YaHei\", sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.5
  reading:
    fontFamily: "\"Segoe UI\", \"Microsoft YaHei\", sans-serif"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.7
  label:
    fontFamily: "\"Segoe UI\", \"Microsoft YaHei\", sans-serif"
    fontSize: "12px"
    fontWeight: 600
    lineHeight: 1.5
rounded:
  badge: "4px"
  chip: "6px"
  control: "7px"
  cover: "8px"
  search: "9px"
  surface: "12px"
  dialog: "16px"
spacing:
  "6": "6px"
  "8": "8px"
  "12": "12px"
  "14": "14px"
  "16": "16px"
  "18": "18px"
  "20": "20px"
  "24": "24px"
components:
  button-primary:
    backgroundColor: "{colors.text}"
    textColor: "#15161b"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "9px 16px"
  button-secondary:
    backgroundColor: "{colors.surface}"
    textColor: "#c9ccd7"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "9px 16px"
  install-button:
    backgroundColor: "#ecedf2"
    textColor: "#191a20"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "8px 13px"
  search-field:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    rounded: "{rounded.search}"
    padding: "5px 7px 5px 16px"
  footer-link:
    backgroundColor: "transparent"
    textColor: "{colors.muted}"
    padding: "5px 0"
  date-control:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.text}"
    rounded: "{rounded.control}"
    height: "36px"
  calendar-day:
    backgroundColor: "transparent"
    textColor: "{colors.text}"
    rounded: "{rounded.chip}"
    padding: "5px 0"
    height: "36px"
  calendar-day-selected:
    backgroundColor: "{colors.pink}"
    textColor: "#22131b"
    rounded: "{rounded.chip}"
    padding: "5px 0"
    height: "36px"
  chart-tag:
    backgroundColor: "#ffffff07"
    textColor: "{colors.note}"
    rounded: "{rounded.chip}"
    padding: "4px 9px"
  chart-tag-selected:
    backgroundColor: "{colors.pink-soft}"
    textColor: "#ffc0de"
    rounded: "{rounded.chip}"
    padding: "4px 9px"
  chart-card:
    backgroundColor: "{colors.card}"
    rounded: "{rounded.surface}"
  review-toggle:
    backgroundColor: "#ffffff05"
    textColor: "#b6bac7"
    rounded: "{rounded.chip}"
    padding: "5px 8px"
  review-overlay-temporary:
    backgroundColor: "#25262e"
    textColor: "{colors.reading}"
    padding: "16px 18px"
  review-drawer-pinned:
    backgroundColor: "#ffffff02"
    textColor: "{colors.reading}"
    padding: "13px 24px 16px"
---

# Design System: SpinShare Browser

## Overview

**Creative North Star: "克制的深色谱面客户端"**

在石墨色层次上组织歌曲、作者和安装操作，用封面与标题帮助识别，用稳定的单列阅读顺序容纳长内容。沿用 SpinShare Browser 名称、既有标志和品红识别色；视觉方向来自用户认可的 Epic 类客户端感觉。

这是基于 2026-09-01 第二轮修订（revision 2）持续更新的设计记录，适用于 codex/chart-tags-and-notes；本轮目录缓存与刷新、说明预览、全文浮层与评论规则优先于下方明确标记为 superseded 的历史方案。谱面主页面沿用 Operate 方向与原设计 seed `cc6cd6ab`，优先保证连续筛选和阅读；当前视觉方案仍待用户审核，不代表最终批准，也不授权合并 main。

第二轮修订只保留谱面主页面，官网、官方客户端和社区移到页脚作为补充链接。首屏直接呈现紧凑、顶端对齐的筛选；SpinShare Browser 保持同一层级的完整名称。共享日历使用与外框一致的深色控件；回顶部的 DOM 和视觉位置统一在顶栏标志处，滚动后只替代图标，名称始终保留。

后续修订已由用户明确授权：评论默认收起但数量后台加载和缓存；数量按钮打开单个不占页面布局的临时浮层，“展开所有评论”则保持完整评论内嵌。两种呈现只以位置、背景和层级区分，不显示模式文字标识。评论时间去除时区尾缀，日期换算不变。结果工具栏保留独立安装状态筛选，核对本地候选集合而不额外刷新远程目录。这些变更保留原有深色方向、页面结构和安全约束。

作者说明保留紧凑卡片布局：短说明完整显示，长说明以真实文字半行裁断和末尾渐隐提示全文，仅主动点击或键盘确认后打开独立阅读浮层。长预览的文字与实体表面轻微抬升，背后用扩散柔光表达层次；外部命中区域不动，悬停不自动打开。描述浮层只显示正文，曲目上下文留给可访问名称，评论标题不变。评论计数与两种展开方式继续保留，排序不再提供评论数量选项。

全量目录与十分钟请求间隔持久保存，正常完全退出后保留；筛选和“刷新列表”自动复用本地数据，到期才更新，不把正常冷却变成可见倒计时或禁用状态。真正的更新故障应说明正在使用旧目录，没有可用数据时显示真实错误；不以空表掩盖失败。

依据 [PRODUCT.md](PRODUCT.md)、[web/index.html](web/index.html)、[web/interface.css](web/interface.css)、[web/chart-card.js](web/chart-card.js) 与 [web/app.js](web/app.js)。前置 YAML 记录实际复用值；版式与行为以下述说明为准，不新增运行时主题层。

**Key Characteristics:**

- 石墨色分层，控件安静，功能状态清晰。
- 系统字体与紧凑密度，歌曲识别优先。
- 单一主页面、同层级名称与直接可用的筛选，减少重复标题和导航。
- 长标题自然换行，长说明主动打开全文，评论按需展开且完整可达。

## Colors

中性石墨层从页面、顶栏与筛选区、卡片逐级升到控件；文字使用明亮主色与柔和次色。颜色值仅在前置 tokens 定义。

- **Primary：** `pink` 与 `pink-soft` 承担有意义的强调，绿色 `green` 表示已安装、推荐或成功。
- **Neutral：** `bg` 为页面底，`sidebar` 为顶栏与筛选区沿用的色彩 token 名称，并不表示存在侧栏；`card` 为谱面，`surface` / `surface-hover` 为控件及其交互状态。
- **Text：** `text` 用于主信息，`muted` 用于辅助信息，`reading` / `note` 用于评论与作者说明。低透明度 `line` 分隔卡片内部区域。
- **Feedback：** `error` 配合具体错误文案；不要只靠颜色解释安装或请求状态。

**The 功能强调 Rule.** 品红用于选中、焦点、进度及现有链接、难度和提示状态；不铺成装饰性大色块。主要筛选与安装按钮使用浅色底。

## Typography

全界面采用前置字体栈，无 Google Fonts、下载字体或单独的展示字体。路径字段是现有例外：Consolas / monospace，正文大小（12px）、行高（1.7）。

- `brand` 用于完整名称 SpinShare Browser；两词始终使用相同字号、字重和颜色，视口不超过（1000px / 680px）时整体降为（16px / 15px）。页面的“谱面浏览”一级标题仅供辅助技术读取，不作为可见的大标题。
- `title` 是歌曲标题；卡片内容宽度不超过（460px）时降为（17px）。
- `body` 是全局默认；`reading` 用于说明和评论，评论最大行宽（96ch）。
- `label` 用于主要按钮。辅助控件通常为（11–12px），统计和徽标为（10–11px）；这些紧凑信息不是新的正文尺寸。
- 日期、难度与统计使用等宽数字；保留用户内容换行和任意长词回流，不改写为界面翻译文本。

**The 文字完整 Rule.** 标题与艺人自然换行，说明和评论保留全文；长说明仅在卡片预览处半行裁断，全文通过主动打开浮层访问，不改成摘要。

## Layout

页面只有一个谱面主页面和一个主文档滚动区，不设常驻侧栏。桌面固定顶栏（64px）；主体居中，最大宽度（1640px），默认内边距为上（24px）、两侧（36px）、下（120px）。首屏不插入可见的“谱面浏览”大标题，直接进入筛选。筛选、搜索、结果工具、已选标签和分页沿文档顺序排列；筛选区可收起，标签非空时才显示并吸顶在顶栏下。官网、官方客户端和社区位于内容末尾页脚，不作为应用内页面导航。

筛选分组仅保留一层可见标签；fieldset 的 legend 保留为 sr-only 语义。难度与日期分组均从顶部对齐，展开自定义日期也不改变对齐方式。宽屏中主筛选按钮与输入控件同一行，辅助状态文字可以另起行；缩窄到双列或单列时，操作区按下面的断点换行。

当前浏览页使用单列横卡；卡片内左边为封面、标题、副标题、艺人、难度和作者，右边为说明及一行标签，统计与安装操作位于底部。评论节点仍保留在卡片 DOM 内；默认临时阅读以顶层浮层呈现，不占卡片布局，只有全局展开时完整评论接在操作区之后。默认封面为（112px）方形且使用 cover 裁切；标题区内边距（20px 24px 18px），左右内容比例（0.95:1）、间距（28px）。无说明和标签时，歌曲区占满；不要预留空的说明列。

歌曲信息内部采用网格：封面跨标题 / 艺人和难度两行，文字位于右列，署名放在下一行并横跨整个歌曲信息区。标题继续自然换行，不裁切文字，也不把署名挤在封面右侧而留下大块空白。统计区分为指标组与操作组；安装状态和下载按钮始终属于同一操作行并垂直居中对齐。日期参与指标组的自然换行，不强制独占一行。

| 响应条件 | 实际变化 |
| --- | --- |
| 视口 ≥1900px | 主体两侧（48px）；封面维持默认尺寸；标题区比例（0.8:1）、间距（40px）。 |
| 视口 ≤1100px | 主体两侧（26px）；筛选改两列、操作独立一行，分页重新分行。 |
| 视口 ≤1000px | 主体两侧（22px）；完整名称整体缩小，结果工具间距收紧。 |
| 卡片内容宽 ≤700px | 歌曲与说明上下排列，分隔线从左侧移到上方；卡片横向内边距（20px）。隐藏统计文字标签，保留数字、图标与按钮可访问名称。 |
| 卡片内容宽 ≤560px | 统计指标和操作组明确分成两行；安装状态和下载按钮保持同一行、垂直居中对齐。 |
| 卡片内容宽 ≤460px | 封面（92px），卡片横向内边距（16px）；歌曲信息网格收紧，署名仍占全宽，评论作者与正文上下排列。 |
| 视口 ≤680px | 顶栏（58px），主体两侧（14px）；筛选和分页单列，页脚分行，原生窗口按钮缩窄；语言仍可在设置中选择。 |

卡片宽度使用容器查询，不能把（700px / 560px / 460px）误作视口断点。页面不应横向滚动；长标签在专属浮层换行，长目录和错误消息可断行。视口（900px）另用于 JavaScript 分页按钮密度，不改变卡片布局。

## Elevation & Depth

主体以色阶和细分隔线表达层次，不使用玻璃背景或卡片投影。标签选择、完整标签、帮助浮层和日期日历使用共用抬升表面；描述与评论阅读浮层使用轻度背景压暗与浮层深度集中阅读注意力，设置和确认对话框使用更深阴影与暗化背景。回顶部留在顶栏，不使用单独的悬浮阴影。具体阴影与运动参数记录在 `.impeccable/design.json`，不添加前置 token 组。

**The 静态分层 Rule.** 整张谱面卡片不靠阴影或悬停位移强调层级；长说明预览仅抬升内部表面，外部命中区域不动，浮层和对话框使用阴影表达覆盖关系。

吸顶标签为（25），顶栏为（30），底部安装队列为（40）；回顶部（35）位于顶栏内部层叠上下文。原生 popover / dialog 进入浏览器顶层，避免被卡片裁剪。不要单靠提高 z-index 解决功能控件互相覆盖。

## Shapes

小圆角控件、略大的内容容器与圆形头像共同构成现有形态。前置 `rounded` 区分徽标、标签、控件、封面、搜索、表面和对话框。封面与回顶部使用同级圆角，窄卡封面使用控件级圆角，日历日期使用标签级圆角；头像保持圆形。复用细描边 SVG 图标，不引入图标字体。

## Components

### Buttons and inputs

主筛选按钮与安装按钮是浅底深字；次按钮采用控件底色。普通按钮最小高度（36px），安装按钮（34px）；搜索框默认（48px），窄窗（44px）。悬停只改变颜色，禁用状态降低透明度并保持明确语义；不增加悬停放大。

键盘焦点为品红外轮廓（2px，偏移 3px），搜索框用 focus-within 边框；卡片标签采用向内偏移的轮廓。已有原生输入、选择、details 和 dialog 语义需要保留。强制颜色模式用系统颜色补充边框与选中状态。

### Catalog reuse and refresh

结果区的次级操作命名为“刷新列表”（Refresh list），与“筛选谱面”共用目录检查流程，不是强制绕过服务器间隔的操作。首次及重新启动后的首次筛选从本地 `/v1/charts` 恢复保存的全量目录；已有副本未到期时直接复用，到期时由本地服务刷新并保存后再筛选。文字、标签、排序和分页只使用已加载目录，不额外触发全量读取，也不新增后台定时联网。首次筛选、实际加载与退出时的可用性规则保留，不能因普通冷却禁用“刷新列表”或显示十分钟倒计时。

目录、`lastAttemptAt` 与 `fetchedAt` 保存在应用状态目录的 `charts-cache.json`，默认目录为 `%LOCALAPPDATA%\SpinShareBrowser`，与程序安装目录和谱面目标目录独立，正常完全退出不删除。服务在请求前保存尝试时间，失败与重新启动仍遵守至少十分钟间隔；新目录先原子写入，成功后才发布，不能让界面使用重启后无法恢复的新副本。

普通复用不显示冷却警告。有旧目录时，真实更新失败、超时或保存失败保留旧结果可用，并用简短状态说明正在使用上次保存的数据；首次无可用缓存、读取损坏或无法安全保存且无旧数据时显示真实错误，不伪装为空结果或成功。取得新目录后失效相关派生缓存并重新计算结果和候选计数；目录更新本身保留标签、关键词及其它搜索选择。此目录间隔与评论手动刷新的 60 秒倒计时分别处理。

### Shared date calendar

起止日期使用同一个自制深色日历，复用原生 popover、select 和 button，不依赖浏览器日期选择面板或第三方日期库。手输格式为 YYYY-MM-DD；任一端点可以留空作为开放范围，快捷日期范围继续可用。合法日期下界为（2020-01-01），上界为 Europe/Berlin 的今日，不随本机时区改成另一个日期。

日历顶端可直接选择年份与月份，也可使用上一月、下一月按钮。日期按周一开始的七列排列；当前端点使用实色品红，有效闭合范围使用淡品红，今日使用细内描边，越界日期禁用。年月和星期名称随界面语言显示，日期按钮有完整的可访问名称；日历宽度随可视区域收缩，位置避开固定顶栏，锚点滚出可视区域时关闭。

日期网格采用一个可 Tab 到达的日期焦点：左右箭头移动一天，上下箭头移动一周，Home / End 移到本周首日 / 末日，PageUp / PageDown 翻月，配合 Shift 翻年，Enter / Space 选择。Escape 关闭并返回当前端点的日历按钮；选择日期或清空当前端点后同样返回按钮，不强制滚动。年份、月份及其它按钮仍遵循原生键盘行为。

共享 popover 在 DOM 中始终紧跟当前端点的 date-control；切换起止端点时移动同一个节点，不将它固定挂在文档末尾。这样浮层即使进入浏览器顶层，Tab 顺序仍接在当前日期控件之后。日历只更新筛选条件，不自行触发查询。

### Chart cards and reviews

卡片不设固定总高度，作者说明仍位于原有紧凑信息区，不显示可见“描述”标题，也不参与搜索。移除说明内嵌滚动：短说明完整显示，没有光圈或展开交互，原生 HTTP(S) 链接照常可用；长说明超过预览容量时必须在真实文字行中间露出半行并以末尾渐隐提示全文。预览容量为五行（8.5em，行高 1.7），裁断优先取该范围内实际文字行的几何中点，无法测量时回退到四行半（7.65em）；容器在中点后额外保留 3px，渐隐只占这段余量，让半行主体保持完整可见。不能落在段落空行，也不能只做整行截断。

长预览在悬停、键盘聚焦或打开期间，以实体表面和文字一起抬升（2px）表达可操作性；外部命中区域保持原位，后方两层扩散柔光向下（1px），不以亮色描边代替背景光晕。实体表面使用（linear-gradient(150deg, #2b2a36, #24252d)）和静态落影（0 8px 16px #0007、0 2px 5px #0004），背后光晕使用（0 0 18px 3px #af86df40、0 9px 30px 2px #807bd938）。仅透明度与 transform 在（180ms）内过渡，不动画化阴影，也不持续循环；减少运动时取消位移。键盘通过表面边缘（#c7b1edb3）提示焦点，强制颜色模式使用 Highlight，普通悬停不显示亮色轮廓。

悬停绝不打开全文，只有点击、Enter 或 Space 才唤出独立描述浮层。长预览内部内容为 inert，不可选择、复制或点击链接，这些操作只在全文浮层内提供。标签保持独立底部一行，标签交互不触发描述；不能把说明区和标签包装成同一个展开触发器。

描述浮层最大宽度（640px），评论浮层保留（680px）；描述不设可见歌名、标题或头部 context 节点，只由 aria-label 保留曲目上下文，正文直接位于内部表面且顶部 margin 为（0），评论浮层标题不变。描述全文只复用已加载的数据与既有安全链接渲染，不新增服务器请求，也不摘要化内容。阅读浮层与临时评论、标签浮层协调，整片表面的普通滚轮只滚内部，包括空白、边缘、短内容以及上下滚动边界，Ctrl+滚轮保留默认行为。描述背景幕为更轻的（#05060a1f），其内部表面与评论共用底色、圆角和阴影；沿用（220ms）进入 /（150ms）退出及减少运动规则，阅读期间不持续漂动。不设 X，鼠标离开来源谱面卡片与浮层后容错（160ms）再关闭，Escape 或外部点击也可关闭。首次页面滚动启动同一退场后即释放 owner，后续连续滚动不得重建动画或延迟隐藏，不改变文档高度。

评论默认收起，卡片统计中的评论数量使用轻背景、细边框的按钮，带随展开状态转向的 chevron。按钮留在指标组的正常布局中，不用负 margin 补偿位置。当前整页、连续模式已渲染批次自动进入现有最多 2 并发的评论队列，优先可见或正在展开的卡片；计数与全文 DOM 分开维护，收起不会取消数量加载。刷新失败保留已有数量，否则显示可重试的未知状态；未获取和失败不能伪装为 0。

有效成功响应仅在缺少 `reviews` 且 `average` 严格为 `false` 时兼容为空集合，其它损坏响应仍报错；已确认总数为 0 时保留统一评论按钮外形，但禁用展开、隐藏箭头且无悬停高亮，全局展开也跳过，有条目的纯评分仍可展开。若打开后才确认 0，立即安全关闭并清除旧正文；焦点原在评论区域或数量按钮时回到本卡标题，并使用 `preventScroll`。

所有谱面的手动“更新评论”共用至少（60 秒）冷却，失败也计入。一个全局计时器同步按钮的禁用状态与剩余秒数，不能为每张卡片分别启动倒计时。首次计数和打开缓存不消耗手动刷新额度；冷却、现有队列、进行中的读取及缓存状态分开处理，不靠换卡片、开合或排队绕过间隔，也不因冷却中断后台计数与缓存阅读。时间戳保存在前端同源 localStorage，页面刷新后恢复剩余时间；范围限于共享该源存储的页面，不承诺不同端口、隔离存储或独立进程间的后端强制限流。

点击数量按钮打开单个临时评论浮层。评论节点仍在所属卡片的 DOM 中，使用原生 `popover=manual` 进入浏览器顶层并固定定位，不锁定 body，也不改变文档高度。宽窗以评论按钮为锚点，根据可用空间选择上方或下方，宽度最多（680px）、高度最多（560px），并限制在视口内，不留难以跨越的间隙。视口宽度不超过（680px）时浮层延伸至距底部（12px），上沿兼顾按钮位置、视口高度与固定顶栏，保持从按钮到浮层的短路径。浮层头部只保留曲目上下文，不设右上关闭键；透明外壳以（3px 0）内边距容纳动效，底色（#25262e）、圆角（12px）和阴影（0 16px 44px #0009）落在内部表面上，背景幕（#05060a2e）轻度压暗且不拦截指针事件。浮层内任何位置的普通滚轮都只控制内部评论列表，包括标题、摘要、空白与正文；到顶、到底或短内容无需滚动时，也不把滚轮传给外层，列表保留 `overscroll-behavior: contain`，不推移下方卡片；Ctrl+滚轮及全局内嵌评论仍放行默认行为。

鼠标离开来源卡片和浮层后柔和退回；再次点击数量按钮、Escape、外部点击、换页或来源卡片退出当前结果也会关闭。卡片与浮层之间移动不得提前收起。键盘和触摸均可阅读与关闭；需要返回评论按钮时使用 `preventScroll`，不让焦点恢复引发滚动。临时浮层的打开、异步全文到达和收起都不得触发页面高度变化或下方卡片位移。

全局“展开所有评论”默认关闭。开启后关闭临时浮层，并在每张卡片内展示完整评论，随内容延伸卡片，不随鼠标离开收起；关闭后恢复按需浮层。浮层通过覆盖层级和背景表达临时阅读，全局模式通过卡片内的位置与背景表达持续阅读，不显示模式文字标识。全局模式下数量按钮表达由开关保持的展开状态，不暗示仍可逐卡关闭。

两种呈现均保留完整评论和仅评分项，不使用摘要或条数截断。限制浮层视口高度只用于内部滚动，不限制内容；全局内嵌评论不设固定总高度。缓存数量和响应可以在收起时更新，正文仅在需要展示时构建；分批构建不是内容上限。未加载、失败与确认无评论分别使用简短状态文字。评论时间不显示“· Europe/Berlin”等时区尾缀，原有日期换算保留。

### Tags and search

首次有效筛选前搜索与添加标签不可用。文本搜索与标签独立，多个已选标签按 AND 组合，改变其他筛选不清除标签。联想只允许现有标签、忽略大小写，并显示叠加当前搜索和已选标签后的结果数；回车与卡片标签点击均可添加。

卡片标签固定一行，溢出时 hover、键盘 focus 或触屏打开完整标签浮层，浮层按钮仍可添加。已选栏自然布局后吸顶；超出一行的已选项通过计数按钮访问。添加卡片标签保持来源位置并飞入筛选栏；删除或清空保存当前视口、页码，焦点移动不触发滚动。

### Installation status filter

结果工具栏中的安装状态默认“全部”，可选择“仅已安装”或“仅未安装”；首次有效筛选前不可用。它与难度、日期、关键词搜索范围、AND 标签及排序共同决定结果，改变任一条件不重置其它条件。控件沿用现有工具栏的字号、焦点、禁用和反馈语义，不额外建立一套视觉样式。

过滤对象是整个候选集合，不只是当前页；安装状态通过本地 `/v1/installations/check` 核对，不触发额外的远程全量刷新。未知、正在核对或核对失败必须与已确认的“未安装”区别表达，不能把缺少确认当成未安装，也不能用成功样式掩盖核对失败。

### Header, footer and back to top

顶栏保留原有标志、同层级的 SpinShare Browser 名称、语言与设置，以及桌面窗口控制。官网、官方客户端和社区只作为页脚补充链接，使用柔和文字和小图标，悬停变亮；不再提供侧栏的当前页选中态。

所有窗口宽度的回顶部都位于顶栏标志原位，按钮 DOM 也在 header 内。出现阈值为滚动距离大于 `max(300px, 0.5 × 视口高度)`，隐藏时不可聚焦。滚动后只淡出标志图像，以回顶部按钮替代；SpinShare Browser 名称始终保留且不分出次级样式，回顶后标志恢复。

桌面按钮为（42px）方形，位置为（left 20px、top 11px）；视口不超过（680px）时为（40px）方形，位置为（left 7px、top 9px）。它不占用卡片操作区域。点击将焦点交还筛选摘要并滚到页首，减少运动时直接滚动。

### Installation, dialogs and motion

安装状态、错误说明和细进度条留在卡片，正在处理的任务另有底部队列；完成态使用绿色。设置、目录选择、窗口控制和退出确认沿用原生服务与既有保护，不能以视觉重做绕过它们。

交互动效使用同一节奏标尺：颜色、淡出和直接反馈为（150ms），短距离位移、开关与折叠提示为（180ms），卡片和浮层进入为（220ms）；只有表达标签移动路径的飞行动效使用（280ms）。大型筛选器的文档布局使用原生 `details` 即时切换，不逐帧动画高度；只让摘要箭头和新显示的内部控件以（180ms）透明度 / 短位移提示状态。描述与临时评论共享的阅读浮层内部表面以（220ms）从触发位置浮出，以（150ms）退回，仅在开合时使用透明度、短距离平移和轻微缩放（0.985）；桌面朝触发位置偏移（10px），窄窗偏移（12px），外壳几何范围保持稳定。阅读期间停稳，不持续晃动，不常驻 `will-change`，不使用推移下方卡片的高度动画；减少运动时不做平移或缩放。全局内嵌模式按内容正常占据文档布局，不能把其布局变化套用到临时浮层。标签飞行为（280ms），并接收（220ms）落点提示。加载旋转保留独立的线性循环速度，不纳入交互时长标尺。仅在内容首次出现或操作反馈时使用；减少运动、页面隐藏或宿主不可见时停用 / 结束运动。无需新增动画库。

## Do's and Don'ts

### Do:

- Do 沿用源码已有的色彩、字号、圆角与系统字体；新文案保持中英文可用。
- Do 保持单一谱面主页面、同层级的完整名称、紧凑且顶端对齐的筛选；外部入口留在页脚。
- Do 保留日期手输、直接年月选择、完整键盘导航和开放范围；共享日历跟随当前端点的 DOM 顺序。
- Do 保留完整评论、独立 AND 标签、候选结果数和键盘可达的全部标签弹层；删除标签保持视口和页码。
- Do 让长说明在实际文字半行处裁断并渐隐，悬停或聚焦以实体表面抬升和背景柔光表达层次，外部命中区域不动；点击或 Enter / Space 才打开无可见标题的缓存全文，短说明与其链接保持普通内容行为，标签独立。
- Do 在评论收起时仍后台加载数量，独立缓存；失败保留已知数量，没有数量时明确显示可重试的未知状态；已确认零条保留按钮外形但禁用展开、隐藏箭头与悬停高亮，全局展开跳过，有条目的纯评分仍可展开。
- Do 让所有谱面共用 60 秒手动评论刷新冷却，失败也计入；用单一计时器和同源 localStorage 时间戳同步倒计时，不影响首次计数或缓存阅读。
- Do 用位置、轻度背景压暗和层级区分单个临时浮层与全局内嵌评论；默认收起，全文和键盘关闭均可达，不设右上关闭键。
- Do 保持临时浮层开合前后的页面高度和卡片位置不变；宽窗靠近按钮，窄窗延伸至近底部且保持按钮可达；整个浮层的普通滚轮仅滚动内部，到边界或短内容也不带动外层，阅读期间保持静止。
- Do 让安装状态与其它条件独立组合，核对整个候选集合，并明确区分未知、核对失败与未安装。
- Do 让长内容自然回流，在宽桌面、窄桌面和窄窗检查安装按钮、设置与窗口控制均可操作。
- Do 让歌曲署名占信息区全宽；窄卡的统计与操作分行，但安装状态和下载按钮保持同一行对齐，不裁切标题换取空间。
- Do 保留持久目录缓存、评论刷新倒计时、安装队列、并发限制、错误解释及目录与安装保护；筛选和“刷新列表”自动复用或到期更新，普通目录冷却不打扰操作，真实故障与首次无数据明确区分。

### Don't:

- Don't 引入前端框架、远程字体或静态资源路由；保持现有 Python / WebView2 与内联页面组装。
- Don't 自动请求会增加官网浏览量的曲目详情接口；任意两次服务器全量获取至少间隔十分钟，失败、缓存和重启语义不变。
- Don't 截断或摘要化评论，或将其迁入独立详情页；评论节点保留卡片 DOM 归属，临时阅读可进入浏览器顶层。
- Don't 用 0 代替未知或失败的评论计数，也不要因收起正文取消后台计数。
- Don't 再用临时内嵌抽屉或高度动画推移下方卡片，不锁定页面滚动，也不添加模式文字标识。
- Don't 为切换安装状态额外刷新远程全量数据，或将未知、核对失败当成未安装。
- Don't 用目录冷却倒计时或禁用状态阻碍本地筛选，不得发布尚未成功落盘的新目录，也不能把首次没有可用缓存的失败显示为空表。
- Don't 重新加入常驻侧栏、可见的“谱面浏览”大标题或重复分组标签；不要把 Browser 变成次级名称。
- Don't 搜索作者说明，清空其它筛选时连带清空标签，或删除标签后跳回原卡片。
- Don't 让描述预览内嵌滚动、悬停自动打开全文、在空行裁断，或让长预览的 inert 内容抢占选择和链接操作；不要恢复按评论数量排序。
- Don't 让回顶部覆盖安装操作，替换既有标志，或把离线假封面、示例统计当作生产内容。
- Don't 将此文档视为用户最终视觉批准；用户审核前不合并 main。

生产结构仍是 `web/index.html`、`web/interface.css`、`web/chart-card.js`、`web/app.js` 四个源码文件，由 `src/spinshare_portable.py` 按固定白名单拼为单个内联 style / script 页面；语言目录仍为 `web/locales.json`。源码拆分不意味着新增静态服务入口。

设计面板 sidecar 中的组件文字仅为样式示例，衍生色带仅用于色板展示；两者都不是生产内容或新增生产色值。未把未使用的 `--card-hover`、离线预览封面、测试统计和本地会话数据提升为设计规范。最终视觉批准仍未确定。

## Superseded history

以下记录保留设计演进，但不再定义当前目录刷新、说明与临时评论交互：

- **Superseded：早期完整展开。** 评论曾默认全部内嵌展开；现由默认收起和全局“展开所有评论”控制。
- **Superseded：临时内嵌抽屉。** 数量按钮曾在当前卡片内打开抽屉，使用展开（200ms）/收起（150ms）的高度和透明度动画推动下方卡片，离开整张卡片后收起。本轮改为单个原生顶层固定浮层，开合和正文加载不改变页面高度。
- **Superseded：模式文字。** 先前头部显示“临时展开 / 常驻展开”；当前不显示这些标识，只通过位置、背景和层级区分。全局展开继续内嵌，此行为未被废弃。
- **Superseded：展开后才取数量。** 先前收起时不发起评论读取；当前整页或已渲染批次独立后台加载和缓存数量，关闭正文不取消计数，失败不能当作零评论。
- **Superseded：说明内嵌滚动。** 先前说明在卡片内可聚焦滚动；现由短说明完整显示、长说明半行渐隐预览与主动打开全文浮层替代，悬停不自动打开。
- **Superseded：目录刷新倒计时。** 先前“更新数据”显示十分钟倒计时并在冷却时禁用；当前“刷新列表”与筛选自动复用或到期更新持久目录，后台间隔仍保留，评论手动刷新的 60 秒倒计时不变。
